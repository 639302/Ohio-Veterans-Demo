var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import '../checkbox/mms-checkbox.component.js';
import '../icon/mms-icon.component.js';
import '../tooltip/mms-tooltip.component.js';
/**
 * Group wrapper for mms-checkbox elements.
 *
 * Provides a group-level label, helper text, error validation, and propagates
 * visual properties (colorScheme, size, disabled, readonly) to slotted checkboxes.
 *
 * @element mms-checkbox-group
 *
 * @prop {string} label - Group label displayed above checkboxes
 * @prop {string} helperText - Supplementary guidance displayed below checkboxes
 * @prop {boolean} showTooltip - Shows info icon next to label
 * @prop {string} tooltipText - Tooltip content
 * @prop {CheckboxColorScheme} colorScheme - Brand color propagated to all checkboxes
 * @prop {CheckboxSize} size - Size propagated to all checkboxes
 * @prop {boolean} disabled - Disables all checkboxes in the group
 * @prop {boolean} readonly - Makes all checkboxes readonly
 * @prop {boolean} error - Displays error styling and shows error message
 * @prop {string} errorText - Message displayed when error is true
 * @prop {boolean} required - Marks field group as required
 * @prop {string} name - Form field name for the group
 *
 * @slot - Slot for mms-checkbox elements
 */
let MmsCheckboxGroup = class MmsCheckboxGroup extends LitElement {
    constructor() {
        super(...arguments);
        // ── Group Metadata ──────────────────────────────────────────
        this.label = '';
        this.helperText = '';
        this.name = '';
        // ── Tooltip ─────────────────────────────────────────────────
        this.showTooltip = false;
        this.tooltipText = '';
        // ── Propagated Visual Props ─────────────────────────────────
        this.colorScheme = 'primary';
        this.size = 'md';
        this.disabled = false;
        this.readonly = false;
        // ── Validation ──────────────────────────────────────────────
        this.error = false;
        this.errorText = '';
        this.required = false;
    }
    /**
     * Propagate group-level props to all slotted mms-checkbox elements.
     */
    _propagateToCheckboxes() {
        const slot = this.shadowRoot?.querySelector('slot');
        if (!slot)
            return;
        const checkboxes = slot
            .assignedElements({ flatten: true })
            .filter((el) => el.tagName === 'MMS-CHECKBOX');
        checkboxes.forEach((checkbox) => {
            // Propagate colorScheme
            checkbox.setAttribute('color-scheme', this.colorScheme);
            // Propagate size
            checkbox.setAttribute('size', this.size);
            // Propagate disabled
            if (this.disabled) {
                checkbox.setAttribute('disabled', '');
            }
            else {
                checkbox.removeAttribute('disabled');
            }
            // Propagate readonly
            if (this.readonly) {
                checkbox.setAttribute('readonly', '');
            }
            else {
                checkbox.removeAttribute('readonly');
            }
            // Set name for form grouping
            if (this.name) {
                checkbox.setAttribute('field-name', this.name);
            }
        });
    }
    _handleSlotChange() {
        this._propagateToCheckboxes();
    }
    updated(changedProperties) {
        super.updated(changedProperties);
        // Re-propagate whenever propagated props change
        if (changedProperties.has('colorScheme') ||
            changedProperties.has('size') ||
            changedProperties.has('disabled') ||
            changedProperties.has('readonly') ||
            changedProperties.has('name')) {
            this._propagateToCheckboxes();
        }
    }
    render() {
        return html `
      ${this.label
            ? html `
            <div class="legend" role="group" aria-labelledby="group-label">
              <span id="group-label">${this.label}</span>
              ${this.required
                ? html `<span class="required-indicator" aria-hidden="true">*</span>`
                : nothing}
              ${this.showTooltip
                ? html `
                    <mms-tooltip text="${this.tooltipText}">
                      <mms-icon
                        class="tooltip-icon"
                        name="info"
                        size="sm"
                        aria-label="${this.tooltipText || 'More information'}"
                      ></mms-icon>
                    </mms-tooltip>
                  `
                : nothing}
            </div>
          `
            : nothing}

      <div class="items-container" role="group" aria-labelledby=${this.label ? 'group-label' : nothing}>
        <slot @slotchange=${this._handleSlotChange}></slot>
      </div>

      ${this.helperText && !this.error
            ? html `<span class="helper-text">${this.helperText}</span>`
            : nothing}

      ${this.error && this.errorText
            ? html `
            <span class="error-row" role="alert">
              <svg
                class="error-icon"
                viewBox="0 0 16 16"
                fill="none"
                xmlns="http://www.w3.org/2000/svg"
                aria-hidden="true"
              >
                <path
                  d="M8 1.33334C4.32 1.33334 1.33334 4.32001 1.33334 8.00001C1.33334 11.68 4.32 14.6667 8 14.6667C11.68 14.6667 14.6667 11.68 14.6667 8.00001C14.6667 4.32001 11.68 1.33334 8 1.33334ZM8.66667 11.3333H7.33334V10H8.66667V11.3333ZM8.66667 8.66668H7.33334V4.66668H8.66667V8.66668Z"
                  fill="currentColor"
                />
              </svg>
              ${this.errorText}
            </span>
          `
            : nothing}
    `;
    }
};
MmsCheckboxGroup.styles = css `
    /* ═══════════════════════════════════════════════════════════════════════
       HOST
       ═══════════════════════════════════════════════════════════════════════ */
    :host {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs2);
      font-family: var(--type-body-md-family);
    }

    :host([disabled]) {
      cursor: not-allowed;
    }

    /* ═══════════════════════════════════════════════════════════════════════
       LEGEND — Group label with optional required indicator
       ═══════════════════════════════════════════════════════════════════════ */
    .legend {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs2);
      font-size: var(--type-body-md-size);
      line-height: var(--type-body-md-line-height);
      color: var(--color-text-default);
    }

    :host([disabled]) .legend {
      color: var(--color-disabled-text);
    }

    .required-indicator {
      color: var(--color-input-border-error);
    }

    /* ═══════════════════════════════════════════════════════════════════════
       TOOLTIP ICON — Slotted trigger content for mms-tooltip
       ═══════════════════════════════════════════════════════════════════════ */
    .tooltip-icon {
      flex-shrink: 0;
      width: 16px;
      height: 16px;
      color: var(--color-text-subtle);
    }

    :host([disabled]) .tooltip-icon {
      color: var(--color-disabled-text);
    }

    /* ═══════════════════════════════════════════════════════════════════════
       ITEMS CONTAINER — Vertical stack of checkboxes
       ═══════════════════════════════════════════════════════════════════════ */
    .items-container {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs2);
    }

    /* ═══════════════════════════════════════════════════════════════════════
       HELPER TEXT
       ═══════════════════════════════════════════════════════════════════════ */
    .helper-text {
      font-size: var(--type-ui-caption-size);
      line-height: var(--type-ui-caption-line-height);
      color: var(--color-text-subtle);
    }

    :host([disabled]) .helper-text {
      color: var(--color-disabled-text);
    }

    /* ═══════════════════════════════════════════════════════════════════════
       ERROR MESSAGE
       ═══════════════════════════════════════════════════════════════════════ */
    .error-row {
      display: flex;
      align-items: flex-start;
      gap: var(--spacing-xs2);
      font-size: var(--type-ui-caption-size);
      line-height: var(--type-ui-caption-line-height);
      color: var(--color-input-border-error);
    }

    .error-icon {
      flex-shrink: 0;
      width: 16px;
      height: 16px;
      color: var(--color-input-border-error);
    }

    /* ═══════════════════════════════════════════════════════════════════════
       DENSITY VARIANTS
       Compact density reduces spacing between items for tighter UI
       ═══════════════════════════════════════════════════════════════════════ */
    :host([data-density='compact']) {
      gap: var(--spacing-xs1);
    }

    :host([data-density='compact']) .items-container {
      gap: var(--spacing-xs1);
    }
  `;
__decorate([
    property({ type: String })
], MmsCheckboxGroup.prototype, "label", void 0);
__decorate([
    property({ type: String, attribute: 'helper-text' })
], MmsCheckboxGroup.prototype, "helperText", void 0);
__decorate([
    property({ type: String })
], MmsCheckboxGroup.prototype, "name", void 0);
__decorate([
    property({ type: Boolean, reflect: true, attribute: 'show-tooltip' })
], MmsCheckboxGroup.prototype, "showTooltip", void 0);
__decorate([
    property({ type: String, attribute: 'tooltip-text' })
], MmsCheckboxGroup.prototype, "tooltipText", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'color-scheme' })
], MmsCheckboxGroup.prototype, "colorScheme", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsCheckboxGroup.prototype, "size", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckboxGroup.prototype, "disabled", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckboxGroup.prototype, "readonly", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckboxGroup.prototype, "error", void 0);
__decorate([
    property({ type: String, attribute: 'error-text' })
], MmsCheckboxGroup.prototype, "errorText", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckboxGroup.prototype, "required", void 0);
MmsCheckboxGroup = __decorate([
    customElement('mms-checkbox-group')
], MmsCheckboxGroup);
export { MmsCheckboxGroup };
//# sourceMappingURL=mms-checkbox-group.component.js.map