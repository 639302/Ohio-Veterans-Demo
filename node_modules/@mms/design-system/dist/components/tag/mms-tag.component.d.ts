import { LitElement } from 'lit';
import type { ColorScheme } from '../../config/theme-constraints.js';
import '../icon/mms-icon.component.js';
export type TagStyle = 'stroke' | 'fill';
/**
 * `error` is deliberately excluded from mms-tag's colorScheme — it's a semantic
 * status meaning, not a neutral brand/structural color choice. Button clubs
 * `error` into its colorScheme as a convenience for destructive actions, but
 * that doesn't generalize to a non-interactive, non-semantic label. A tag
 * communicating semantic status (success, error, caution, informational)
 * should use mms-status-tag's `alert-type` instead.
 */
export type TagColorScheme = Exclude<ColorScheme, 'error'>;
/**
 * Compact label representing a property, category, or metadata. Not interactive.
 *
 * @element mms-tag
 *
 * @prop {TagStyle} tagStyle - Visual style: stroke (outlined, default) or fill (solid)
 * @prop {TagColorScheme} colorScheme - Color palette for tag-style="fill": primary, secondary, accent (branded), or onyx (structural black). No effect on tag-style="stroke", which stays neutral. Brand schemes vary by theme. "error" is intentionally not offered — use mms-status-tag's alert-type for semantic status.
 * @prop {string} label - Tag label text
 * @prop {string} leftIcon - Icon name (from Phosphor registry) for left position
 * @prop {string} rightIcon - Icon name (from Phosphor registry) for right position
 */
export declare class MmsTag extends LitElement {
    tagStyle: TagStyle;
    colorScheme: TagColorScheme;
    label: string;
    leftIcon: string;
    rightIcon: string;
    static styles: import("lit").CSSResult;
    render(): import("lit-html").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        'mms-tag': MmsTag;
    }
}
//# sourceMappingURL=mms-tag.component.d.ts.map