var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import '../icon/mms-icon.component.js';
/**
 * Compact label representing a property, category, or metadata. Not interactive.
 *
 * @element mms-tag
 *
 * @prop {TagStyle} tagStyle - Visual style: stroke (outlined, default) or fill (solid)
 * @prop {TagColorScheme} colorScheme - Color palette for tag-style="fill": primary, secondary, accent (branded), or onyx (structural black). No effect on tag-style="stroke", which stays neutral. Brand schemes vary by theme. "error" is intentionally not offered — use mms-status-tag's alert-type for semantic status.
 * @prop {string} label - Tag label text
 * @prop {string} leftIcon - Icon name (from Phosphor registry) for left position
 * @prop {string} rightIcon - Icon name (from Phosphor registry) for right position
 */
let MmsTag = class MmsTag extends LitElement {
    constructor() {
        super(...arguments);
        this.tagStyle = 'stroke';
        this.colorScheme = 'primary';
        this.label = '';
        this.leftIcon = '';
        this.rightIcon = '';
    }
    render() {
        return html `
      ${this.leftIcon
            ? html `<span class="icon"><mms-icon name=${this.leftIcon} size="sm" weight="regular"></mms-icon></span>`
            : nothing}
      <span class="label">${this.label}</span>
      ${this.rightIcon
            ? html `<span class="icon"><mms-icon name=${this.rightIcon} size="sm" weight="regular"></mms-icon></span>`
            : nothing}
    `;
    }
};
MmsTag.styles = css `
    /* data-density is intentionally not implemented here — mms-tag is a static,
       non-interactive label with no touch-target constraint, unlike mms-button's
       compact override. See .claude/contracts/global-props.md. */

    /* Color Scheme mapping — only consumed by tag-style="fill" below. Unlike
       mms-button, no hover/pressed step (10/11) is needed since mms-tag is
       non-interactive, so no Tier 2/3 fallback chain is required either —
       step 9 (the solid fill) exists on every tier. */
    :host {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: var(--spacing-xs2);
      padding: 4px var(--spacing-sm2);
      border-radius: var(--radius-full);
      box-sizing: border-box;
      font-family: var(--type-body-sm-family);
      font-size: var(--type-body-sm-size);
      font-weight: var(--type-body-sm-weight);
      line-height: var(--type-body-sm-line-height);

      --_solid: var(--color-primary-9);
      --_on-solid: var(--color-text-on-primary);
    }

    :host([color-scheme='secondary']) {
      --_solid: var(--color-secondary-9);
      --_on-solid: var(--color-text-on-secondary);
    }

    :host([color-scheme='accent']) {
      --_solid: var(--color-accent-9);
      --_on-solid: var(--color-text-on-accent);
    }

    :host([color-scheme='onyx']) {
      --_solid: var(--color-onyx-9);
      --_on-solid: var(--color-text-on-onyx);
    }

    /* Stroke style (default) — neutral regardless of colorScheme */
    :host,
    :host([tag-style='stroke']) {
      background: transparent;
      border: var(--border-width-xs) solid var(--color-border-default);
      color: var(--color-text-default);
    }

    /* Fill style */
    :host([tag-style='fill']) {
      background: var(--_solid);
      border: var(--border-width-xs) solid transparent;
      color: var(--_on-solid);
    }

    .icon {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 16px;
      height: 16px;
      flex-shrink: 0;
      color: inherit;
    }

    /* Scale icon visual to 14x14 (matches label font-size) while keeping the 16px hitbox */
    .icon mms-icon {
      transform: scale(0.875);
    }

    .label {
      white-space: nowrap;
    }
  `;
__decorate([
    property({ type: String, reflect: true, attribute: 'tag-style' })
], MmsTag.prototype, "tagStyle", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'color-scheme' })
], MmsTag.prototype, "colorScheme", void 0);
__decorate([
    property({ type: String })
], MmsTag.prototype, "label", void 0);
__decorate([
    property({ type: String, attribute: 'left-icon' })
], MmsTag.prototype, "leftIcon", void 0);
__decorate([
    property({ type: String, attribute: 'right-icon' })
], MmsTag.prototype, "rightIcon", void 0);
MmsTag = __decorate([
    customElement('mms-tag')
], MmsTag);
export { MmsTag };
//# sourceMappingURL=mms-tag.component.js.map