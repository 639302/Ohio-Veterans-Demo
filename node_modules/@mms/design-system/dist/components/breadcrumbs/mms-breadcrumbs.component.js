var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import '../link/mms-link.component.js';
/**
 * Breadcrumb trail with overflow truncation.
 *
 * Marina reference: `ds-breadcrumb` + `ds-breadcrumb-item` (chubb-design-system).
 * Marina renders two custom elements (a container plus one child element per
 * crumb); mms-breadcrumbs folds both into a single data-driven component —
 * consistent with a `items` array being the natural shape for a trail
 * (order and truncation are structural, not something a consumer composes
 * via light-DOM slotting the way `mms-checkbox-group` composes independent
 * `mms-checkbox` children).
 *
 * Implementation notes (see DS-061 token mapping / PR notes for full detail):
 * - Renders the WAI-ARIA APG breadcrumb landmark (`nav[aria-label] > ol > li`)
 *   with `aria-current="page"` on the current crumb.
 * - No host-level `tabindex="0"` — the real interactive elements are the
 *   composed `mms-link` crumbs, each with their own tab stop and focus ring.
 * - The overflow ("…") crumb renders as static, non-interactive text — no
 *   overflow menu is implemented, so a focusable no-op control would be a
 *   keyboard trap with no purpose.
 * - Sub-item crumbs use `link-color="neutral"` (inherits surrounding text
 *   color), matching mms's own documented guidance for breadcrumbs/footers
 *   (see mms-link Overview story).
 * - `maxItems` governs the truncated count (`first + overflow +
 *   last(maxItems - 2)`, floored at 1 trailing item) — a prop named
 *   `maxItems` that doesn't control the rendered count would be misleading.
 *
 * @element mms-breadcrumbs
 *
 * @prop {BreadcrumbItem[]} items - Ordered list of crumbs, root first.
 * @prop {number} maxItems - Truncation threshold. Above this count, renders
 *   `first + overflow + last(maxItems - 2)` (floored at 1 trailing item so
 *   the current-page crumb is never dropped).
 * @prop {BreadcrumbsSize} size - Text size (`sm` | `md`).
 */
let MmsBreadcrumbs = class MmsBreadcrumbs extends LitElement {
    constructor() {
        super(...arguments);
        this.items = [];
        this.maxItems = 4;
        this.size = 'md';
    }
    get _renderItems() {
        const items = this.items ?? [];
        const max = this.maxItems;
        if (max > 0 && items.length > max) {
            // first + overflow + trailing items sized to max (floor of 1 so the
            // current-page crumb is never dropped).
            const lastCount = Math.max(max - 2, 1);
            return [items[0], { label: '…', type: 'overflow' }, ...items.slice(-lastCount)];
        }
        return items;
    }
    render() {
        const renderItems = this._renderItems;
        const lastIndex = renderItems.length - 1;
        return html `
      <nav aria-label="Breadcrumb">
        <ol>
          ${renderItems.map((item, index) => {
            const type = item.type ?? (index === lastIndex ? 'current-page' : 'sub-item');
            const label = item.label ?? item.text ?? 'Link';
            return html `
              <li>
                ${type === 'current-page'
                ? html `<span class="crumb-current" aria-current="page">${label}</span>`
                : type === 'overflow'
                    ? html `<span class="crumb-overflow">${label}</span>`
                    : html `
                        <mms-link
                          href=${item.href ?? nothing}
                          label=${label}
                          size=${this.size}
                          link-color="neutral"
                        ></mms-link>
                      `}
                ${index < lastIndex ? html `<span class="separator" aria-hidden="true">/</span>` : nothing}
              </li>
            `;
        })}
        </ol>
      </nav>
    `;
    }
};
MmsBreadcrumbs.styles = css `
    :host {
      display: block;
      font-family: var(--type-body-md-family);
    }

    ol {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      margin: 0;
      padding: 0;
      list-style: none;
    }

    li {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs2);
    }

    .crumb-current {
      font-weight: 700;
      color: var(--color-text-default);
    }

    .crumb-overflow {
      color: var(--color-text-subtle);
    }

    .separator {
      color: var(--color-text-subtle);
      padding: 0 var(--spacing-xs2);
    }

    :host([size='sm']) .crumb-current,
    :host([size='sm']) .crumb-overflow,
    :host([size='sm']) .separator {
      font-size: var(--type-body-sm-size);
      line-height: var(--type-body-sm-line-height);
    }

    :host([size='md']) .crumb-current,
    :host([size='md']) .crumb-overflow,
    :host([size='md']) .separator {
      font-size: var(--type-body-md-size);
      line-height: var(--type-body-md-line-height);
    }

    :host([data-density='compact']) li {
      gap: var(--spacing-xs1);
    }

    :host([data-density='compact']) .separator {
      padding: 0 var(--spacing-xs1);
    }
  `;
__decorate([
    property({ type: Array })
], MmsBreadcrumbs.prototype, "items", void 0);
__decorate([
    property({ type: Number, attribute: 'max-items' })
], MmsBreadcrumbs.prototype, "maxItems", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsBreadcrumbs.prototype, "size", void 0);
MmsBreadcrumbs = __decorate([
    customElement('mms-breadcrumbs')
], MmsBreadcrumbs);
export { MmsBreadcrumbs };
//# sourceMappingURL=mms-breadcrumbs.component.js.map