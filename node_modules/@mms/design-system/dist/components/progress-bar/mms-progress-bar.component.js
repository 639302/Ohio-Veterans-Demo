var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
/**
 * showLabel defaults to true — plain HTML boolean-attribute semantics
 * (presence = true) can't express "false" via a string, which breaks
 * show-label="false" for static-HTML consumers (AEM/Drupal) that can't use
 * Lit property binding. This converter treats the literal string "false" as
 * false. Same pattern as mms-toggle's showIcon.
 */
const defaultTrueBooleanConverter = {
    fromAttribute: (value) => value !== 'false',
    toAttribute: (value) => (value ? null : 'false'),
};
/**
 * Determinate progress indicator — represents completion of a user-initiated
 * process. Not interactive; has no keyboard model. For indeterminate loading,
 * see mms-loading-bar.
 *
 * @element mms-progress-bar
 *
 * @prop {string} label - Label text describing the process. Also used as the accessible name via aria-labelledby (or aria-label when showLabel is false).
 * @prop {boolean} showLabel - Show/hide the visible label row (default true). The label is still exposed to assistive technology via aria-label when hidden.
 * @prop {ProgressBarSize} size - Track thickness: sm (8px, default), md (12px), or lg (16px)
 * @prop {number} progress - Progress value, 0-100. Clamped at render.
 * @prop {ProgressBarValuePlacement} valuePlacement - Where the percentage value renders: label-row (above the track, default) or inline (next to the track)
 * @prop {boolean} showCompleteText - Append " complete" to the value text (e.g. "80% complete") instead of just "80%"
 * @prop {ProgressBarColorScheme} colorScheme - Fill color: primary (default), secondary, accent, or onyx. All are solid step-9 fills — since the track carries no overlaid text, every tier (including Tier 3 decorative) satisfies the 3:1 non-text UI contrast requirement, so no theme/colorScheme combination is blocked.
 */
let MmsProgressBar = class MmsProgressBar extends LitElement {
    constructor() {
        super(...arguments);
        this.label = '';
        this.showLabel = true;
        this.size = 'sm';
        this.progress = 0;
        this.valuePlacement = 'label-row';
        this.showCompleteText = false;
        this.colorScheme = 'primary';
    }
    get clampedProgress() {
        return Math.min(100, Math.max(0, this.progress));
    }
    get valueText() {
        const progress = this.clampedProgress;
        return this.showCompleteText ? `${progress}% complete` : `${progress}%`;
    }
    render() {
        const progress = this.clampedProgress;
        const valueText = this.valueText;
        const isInline = this.valuePlacement === 'inline';
        const track = html `
      <div
        class="track-container"
        role="progressbar"
        aria-valuenow=${progress}
        aria-valuemin="0"
        aria-valuemax="100"
        aria-valuetext=${valueText}
        aria-labelledby=${this.showLabel && this.label ? 'label' : nothing}
        aria-label=${!this.showLabel && this.label ? this.label : nothing}
      >
        <div class="bar" style="width: ${progress}%"></div>
      </div>
    `;
        if (isInline) {
            return html `
        ${this.showLabel
                ? html `<div class="label-row"><span class="label" id="label">${this.label}</span></div>`
                : nothing}
        <div class="bar-row">
          ${track}
          <span class="value-inline">${valueText}</span>
        </div>
      `;
        }
        return html `
      ${this.showLabel
            ? html `
            <div class="label-row">
              <span class="label" id="label">${this.label}</span>
              <span class="value">${valueText}</span>
            </div>
          `
            : nothing}
      ${track}
    `;
    }
};
MmsProgressBar.styles = css `
    :host {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-sm1);
      width: 100%;
      font-family: var(--type-body-md-family);
      font-size: var(--type-body-md-size);
      line-height: var(--type-body-md-line-height);
      font-weight: var(--type-body-md-weight);
      color: var(--color-text-default);

      --_solid: var(--color-primary-9);
      --_track-height: 8px;
    }

    :host([size='md']) {
      --_track-height: 12px;
    }

    :host([size='lg']) {
      --_track-height: 16px;
    }

    :host([color-scheme='secondary']) {
      --_solid: var(--color-secondary-9);
    }

    :host([color-scheme='accent']) {
      --_solid: var(--color-accent-9);
    }

    :host([color-scheme='onyx']) {
      --_solid: var(--color-onyx-9);
    }

    .label-row {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: var(--spacing-sm1);
    }

    .value {
      flex-shrink: 0;
    }

    .track-container {
      position: relative;
      width: 100%;
      overflow: hidden;
      border-radius: var(--radius-full);
      background: var(--color-surface-sunken);
      height: var(--_track-height);
    }

    .bar {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      background: var(--_solid);
      border-radius: var(--radius-full);
      transition: width var(--motion-duration-moderate) var(--motion-easing-standard);
    }

    @media (prefers-reduced-motion: reduce) {
      .bar {
        transition-duration: var(--motion-duration-instant);
      }
    }

    .bar-row {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm2);
      /* Vertically centering track against the taller text line-box grows this
         row upward past the track's own height — pulling it back up by that
         same excess keeps the label-to-track gap flush with label-row mode
         while leaving track/text centered relative to each other untouched. */
      margin-top: calc((var(--_track-height) - (var(--type-body-md-line-height) * var(--type-body-md-size))) / 2);
    }

    .bar-row .track-container {
      flex: 1;
    }

    .value-inline {
      flex-shrink: 0;
      white-space: nowrap;
      position: relative;
      top: -1px;
    }
  `;
__decorate([
    property({ type: String })
], MmsProgressBar.prototype, "label", void 0);
__decorate([
    property({ reflect: true, attribute: 'show-label', converter: defaultTrueBooleanConverter })
], MmsProgressBar.prototype, "showLabel", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsProgressBar.prototype, "size", void 0);
__decorate([
    property({ type: Number, reflect: true })
], MmsProgressBar.prototype, "progress", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'value-placement' })
], MmsProgressBar.prototype, "valuePlacement", void 0);
__decorate([
    property({ type: Boolean, reflect: true, attribute: 'show-complete-text' })
], MmsProgressBar.prototype, "showCompleteText", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'color-scheme' })
], MmsProgressBar.prototype, "colorScheme", void 0);
MmsProgressBar = __decorate([
    customElement('mms-progress-bar')
], MmsProgressBar);
export { MmsProgressBar };
//# sourceMappingURL=mms-progress-bar.component.js.map