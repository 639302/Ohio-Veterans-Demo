var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import { ifDefined } from 'lit/directives/if-defined.js';
import { focusRing } from '../../styles/focus.css.js';
import '../icon/mms-icon.component.js';
/**
 * Link component for navigation with USWDS-aligned colors.
 *
 * When `href` is provided, renders a semantic `<a>` element.
 * When `href` is omitted, renders styled text for use inside framework router wrappers.
 *
 * @element mms-link
 *
 * @prop {string} href - Navigation URL. When set, renders as `<a>`. When omitted, renders as styled span.
 * @prop {string} target - Where to open link (_self, _blank, etc.). Only applies when href is set.
 * @prop {string} label - Link text content
 * @prop {LinkSize} size - Text size: sm (body-sm), md (body-md), lg (body-lg)
 * @prop {LinkColor} link-color - Color treatment: standard (USWDS blue), neutral (mode-aware body text).
 *   A third value, `inherit`, resolves to `currentColor` — reserved for internal system-component
 *   composition (e.g. `mms-toast`, which sets a fixed on-fill `color` from outside to match a
 *   non-inverting colored surface). Not exposed in the Link story's Controls — not a general design
 *   choice, since a fixed non-mode-aware color is only correct against a fill that's also fixed.
 * @prop {LinkUnderline} underline - Underline behavior: always, hover (default), none
 * @prop {boolean} disabled - Disabled state
 * @prop {LinkState} state - Preview state for Storybook demos
 * @prop {string} leftIcon - Icon name from registry for left position
 * @prop {string} rightIcon - Icon name from registry for right position
 */
let MmsLink = class MmsLink extends LitElement {
    constructor() {
        super(...arguments);
        this.label = '';
        this.size = 'md';
        this.linkColor = 'standard';
        this.underline = 'hover';
        this.disabled = false;
        this.state = 'default';
        this.leftIcon = '';
        this.rightIcon = '';
        this._handleKeyDown = (e) => {
            if (this.disabled)
                return;
            // Activate on Enter (standard link behavior)
            if (e.key === 'Enter') {
                e.preventDefault();
                this.click();
            }
        };
    }
    connectedCallback() {
        super.connectedCallback();
        // When no href, component needs link semantics
        if (!this.href) {
            if (!this.hasAttribute('role')) {
                this.setAttribute('role', 'link');
            }
            if (!this.hasAttribute('tabindex')) {
                this.setAttribute('tabindex', '0');
            }
            this.addEventListener('keydown', this._handleKeyDown);
        }
    }
    disconnectedCallback() {
        super.disconnectedCallback();
        this.removeEventListener('keydown', this._handleKeyDown);
    }
    updated(changedProperties) {
        super.updated(changedProperties);
        // Update tabindex when disabled changes (for non-href mode)
        if (changedProperties.has('disabled') && !this.href) {
            this.setAttribute('tabindex', this.disabled ? '-1' : '0');
            this.setAttribute('aria-disabled', String(this.disabled));
        }
    }
    /**
     * Compute icon size based on link size
     */
    _getIconSize() {
        switch (this.size) {
            case 'sm':
                return 'sm';
            case 'lg':
                return 'md';
            default:
                return 'sm';
        }
    }
    render() {
        const content = html `
      ${this.leftIcon
            ? html `<mms-icon class="icon" name=${this.leftIcon} size=${this._getIconSize()}></mms-icon>`
            : nothing}
      <span class="label"><slot>${this.label}</slot></span>
      ${this.rightIcon
            ? html `<mms-icon class="icon" name=${this.rightIcon} size=${this._getIconSize()}></mms-icon>`
            : nothing}
    `;
        // Render as <a> when href is provided
        if (this.href) {
            return html `
        <a
          class="link"
          href=${this.href}
          target=${ifDefined(this.target)}
          rel=${this.target === '_blank' ? 'noopener noreferrer' : nothing}
          aria-disabled=${this.disabled ? 'true' : nothing}
        >
          ${content}
        </a>
      `;
        }
        // Render as styled span for framework router wrappers
        return html `<span class="link">${content}</span>`;
    }
};
MmsLink.styles = [
    focusRing,
    css `
      /* ═══════════════════════════════════════════════════════════════════════
         HOST — Display + Variant Color Mapping
         ═══════════════════════════════════════════════════════════════════════ */
      :host {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-xs2);
        font-family: var(--type-body-md-family);
        cursor: pointer;
        text-decoration: none;
        transition:
          color var(--motion-duration-base) var(--motion-easing-standard),
          text-decoration var(--motion-duration-base) var(--motion-easing-standard);

        /* Default: standard variant colors */
        --_link-color: var(--color-link-default);
        --_link-hover: var(--color-link-hover);
        --_link-visited: var(--color-link-visited);
      }

      :host([link-color='neutral']) {
        --_link-color: var(--color-text-default);
        --_link-hover: var(--color-text-default);
        --_link-visited: var(--color-text-default);
      }

      /* Internal-use variant — see the link-color JSDoc above. currentColor
         resolves to whatever color the consumer sets on this host element
         from outside (standard CSS inheritance), so a fixed-fill component
         can hand this link its own fixed on-fill text color without this
         component needing to know what that color is. */
      :host([link-color='inherit']) {
        --_link-color: currentColor;
        --_link-hover: currentColor;
        --_link-visited: currentColor;
      }

      :host([disabled]) {
        cursor: not-allowed;
        pointer-events: none;
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         SIZE VARIANTS
         Maps to body type scale: sm, md, lg
         ═══════════════════════════════════════════════════════════════════════ */
      :host([size='sm']) {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
      }

      :host([size='sm']) .icon {
        width: 14px;
        height: 14px;
      }

      :host([size='md']),
      :host(:not([size])) {
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
      }

      :host([size='md']) .icon,
      :host(:not([size])) .icon {
        width: 16px;
        height: 16px;
      }

      :host([size='lg']) {
        font-size: var(--type-body-lg-size);
        line-height: var(--type-body-lg-line-height);
      }

      :host([size='lg']) .icon {
        width: 18px;
        height: 18px;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         LINK ELEMENT — Native <a> or styled span
         ═══════════════════════════════════════════════════════════════════════ */
      .link {
        display: inline-flex;
        flex-wrap: wrap;
        align-items: center;
        gap: inherit;
        color: var(--_link-color);
        text-decoration: none;
        font: inherit;
        cursor: inherit;
      }

      /* Without this, .label's default flex-item min-width (auto) keeps it at
         its full unwrapped text width, so flex-wrap above has nothing to act
         on — at extreme narrow+zoomed states (WCAG 1.4.4) the label needs to
         both shrink and break within its own text to avoid overflowing past
         a shrunk-down host. */
      .label {
        min-width: 0;
        overflow-wrap: break-word;
      }

      .link:focus-visible {
        outline: var(--focus-ring-width) var(--focus-ring-style) var(--focus-ring-color);
        outline-offset: var(--focus-ring-offset);
        border-radius: 2px;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         UNDERLINE BEHAVIOR
         ═══════════════════════════════════════════════════════════════════════ */
      /* underline='always' — always underlined */
      :host([underline='always']) .link {
        text-decoration: underline;
      }

      /* underline='hover' (default) — underline on hover */
      :host([underline='hover']) .link,
      :host(:not([underline])) .link {
        text-decoration: none;
      }

      :host([underline='hover']:hover:not([disabled])) .link,
      :host(:not([underline]):hover:not([disabled])) .link,
      :host([underline='hover'][state='hover']:not([disabled])) .link,
      :host(:not([underline])[state='hover']:not([disabled])) .link {
        text-decoration: underline;
      }

      /* underline='none' — never underlined */
      :host([underline='none']) .link {
        text-decoration: none;
      }

      :host([underline='none']:hover:not([disabled])) .link,
      :host([underline='none'][state='hover']:not([disabled])) .link {
        text-decoration: none;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         STATE STYLES
         ═══════════════════════════════════════════════════════════════════════ */
      /* Hover — disabled takes precedence */
      :host(:hover:not([disabled])) .link,
      :host([state='hover']:not([disabled])) .link {
        color: var(--_link-hover);
      }

      /* Focus — disabled takes precedence */
      :host([state='focus']:not([disabled])) .link {
        outline: var(--focus-ring-width) var(--focus-ring-style) var(--focus-ring-color);
        outline-offset: var(--focus-ring-offset);
        border-radius: 2px;
      }

      /* Visited (standard variant only) — disabled takes precedence */
      :host([link-color='standard'][state='visited']:not([disabled])) .link,
      :host(:not([link-color])[state='visited']:not([disabled])) .link {
        color: var(--_link-visited);
      }

      /* Disabled */
      :host([disabled]) .link {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         ICON STYLES
         ═══════════════════════════════════════════════════════════════════════ */
      .icon {
        display: inline-flex;
        flex-shrink: 0;
        color: inherit;
      }
    `,
];
__decorate([
    property({ type: String })
], MmsLink.prototype, "href", void 0);
__decorate([
    property({ type: String })
], MmsLink.prototype, "target", void 0);
__decorate([
    property({ type: String })
], MmsLink.prototype, "label", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsLink.prototype, "size", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'link-color' })
], MmsLink.prototype, "linkColor", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsLink.prototype, "underline", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsLink.prototype, "disabled", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsLink.prototype, "state", void 0);
__decorate([
    property({ type: String, attribute: 'left-icon' })
], MmsLink.prototype, "leftIcon", void 0);
__decorate([
    property({ type: String, attribute: 'right-icon' })
], MmsLink.prototype, "rightIcon", void 0);
MmsLink = __decorate([
    customElement('mms-link')
], MmsLink);
export { MmsLink };
//# sourceMappingURL=mms-link.component.js.map