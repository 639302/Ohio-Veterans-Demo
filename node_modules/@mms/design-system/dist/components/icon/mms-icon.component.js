var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property, state } from 'lit/decorators.js';
import { unsafeSVG } from 'lit/directives/unsafe-svg.js';
import { focusRing } from '../../styles/focus.css.js';
// Lazy-load SVG registry — loaded on first render, cached globally.
// This keeps icon-svgs.json (1.3MB) out of the initial bundle.
// Bundlers automatically code-split the dynamic import into a separate chunk.
let ICON_SVGS = {};
let _svgLoadPromise = null;
function loadIconSvgs() {
    if (!_svgLoadPromise) {
        _svgLoadPromise = import('../../icons/icon-svgs.json')
            .then(mod => { ICON_SVGS = (mod.default || mod); })
            .catch(err => { console.warn('mms-icon: failed to load SVG registry', err); });
    }
    return _svgLoadPromise;
}
let MmsIcon = class MmsIcon extends LitElement {
    constructor() {
        super(...arguments);
        this.name = 'question';
        this.size = 'lg';
        this.weight = 'regular';
        this.label = '';
        this.badge = '';
        this._svgsLoaded = false;
    }
    get badgeType() {
        if (!this.badge)
            return null;
        return this.badge === 'dot' ? 'dot' : 'count';
    }
    get badgeText() {
        const n = parseInt(this.badge, 10);
        if (!isNaN(n))
            return n > 99 ? '99+' : String(n);
        return this.badge;
    }
    render() {
        // Trigger lazy load of SVG registry; re-render once loaded
        if (!this._svgsLoaded) {
            loadIconSvgs().then(() => {
                this._svgsLoaded = true;
            });
        }
        const entry = ICON_SVGS[this.name];
        const bType = this.badgeType;
        // Show placeholder if SVGs not loaded yet or icon not found
        if (!entry) {
            return html `<svg
        viewBox="0 0 256 256"
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
      ><rect x="32" y="32" width="192" height="192" fill="none" stroke="currentColor" stroke-width="16" rx="8"/></svg>`;
        }
        const inner = entry[this.weight] ?? entry.regular;
        const isDecorative = !this.label;
        return html `
      <svg
        viewBox="0 0 256 256"
        xmlns="http://www.w3.org/2000/svg"
        ?aria-hidden=${isDecorative}
        role=${isDecorative ? 'presentation' : 'img'}
        aria-label=${isDecorative ? '' : this.label}
      >${unsafeSVG(inner)}</svg>
      ${bType ? html `<span class="badge ${bType}" aria-hidden="true">${bType === 'count' ? this.badgeText : nothing}</span>` : nothing}
    `;
    }
};
MmsIcon.styles = [
    focusRing,
    css `
      :host {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        position: relative;
        width: var(--icon-size-lg, 24px);
        height: var(--icon-size-lg, 24px);
        color: currentColor;
      }
      :host([size='sm']) { width: var(--icon-size-sm, 16px); height: var(--icon-size-sm, 16px); }
      :host([size='md']) { width: var(--icon-size-md, 20px); height: var(--icon-size-md, 20px); }
      :host([size='xl']) { width: var(--icon-size-xl, 32px); height: var(--icon-size-xl, 32px); }

      svg {
        display: block;
        width: 100%;
        height: 100%;
        fill: currentColor;
      }

      .badge {
        position: absolute;
        top: -3px;
        right: -3px;
        background: var(--color-utility-notification);
        border: 1px solid var(--color-utility-on-fill);
        border-radius: 9999px;
        pointer-events: none;
      }

      .badge.dot {
        width: 8px;
        height: 8px;
      }

      .badge.count {
        min-width: 14px;
        height: 14px;
        padding: 0 3px;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        font-size: 8px;
        font-weight: 700;
        line-height: 1;
        color: var(--color-utility-on-fill);
        display: flex;
        align-items: center;
        justify-content: center;
        box-sizing: border-box;
      }
    `,
];
__decorate([
    property({ type: String })
], MmsIcon.prototype, "name", void 0);
__decorate([
    property({ type: String })
], MmsIcon.prototype, "size", void 0);
__decorate([
    property({ type: String })
], MmsIcon.prototype, "weight", void 0);
__decorate([
    property({ type: String })
], MmsIcon.prototype, "label", void 0);
__decorate([
    property({ type: String })
], MmsIcon.prototype, "badge", void 0);
__decorate([
    state()
], MmsIcon.prototype, "_svgsLoaded", void 0);
MmsIcon = __decorate([
    customElement('mms-icon')
], MmsIcon);
export { MmsIcon };
//# sourceMappingURL=mms-icon.component.js.map