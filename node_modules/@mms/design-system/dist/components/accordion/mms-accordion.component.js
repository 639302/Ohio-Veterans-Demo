var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html } from 'lit';
import { customElement, property, state } from 'lit/decorators.js';
import './mms-accordion-item.component.js';
/**
 * Container for mms-accordion-item elements. Propagates shared visual props
 * to slotted items, enforces single-open exclusivity when mode="single",
 * and adds arrow/Home/End keyboard navigation across item headers.
 *
 * Replaces Marina's data-driven `items` array prop with the slotted-children
 * pattern established by mms-radio-group/mms-radio — consumers author
 * mms-accordion-item elements directly rather than passing item objects.
 *
 * @element mms-accordion
 *
 * @prop {AccordionMode} mode - 'single' allows only one item open at a time; 'multiple' allows any number
 * @prop {AccordionCuePosition} cuePosition - Propagated to all items
 * @prop {AccordionCueType} cueType - Propagated to all items
 * @prop {AccordionTitleSize} titleSize - Propagated to all items
 * @prop {boolean} indent - Propagated to all items
 * @prop {AccordionDensity} density - Propagated to all items as `data-density`
 *
 * @slot - Slot for mms-accordion-item elements
 */
let MmsAccordion = class MmsAccordion extends LitElement {
    constructor() {
        super(...arguments);
        this.mode = 'multiple';
        this.cuePosition = 'right';
        this.cueType = 'caret';
        this.titleSize = 'body';
        this.indent = false;
        this.density = 'default';
        this._items = [];
        this._handleSlotChange = () => {
            this._propagateToItems();
        };
        this._handleItemToggle = (e) => {
            if (this.mode !== 'single' || !e.detail.expanded)
                return;
            const target = e.target;
            this._items.forEach((item) => {
                if (item !== target) {
                    item.expanded = false;
                }
            });
        };
        this._handleKeyDown = (e) => {
            const items = this._items;
            if (items.length === 0)
                return;
            const target = e.target;
            const currentIndex = items.indexOf(target);
            if (currentIndex < 0)
                return;
            let newIndex = currentIndex;
            switch (e.key) {
                case 'ArrowDown':
                    e.preventDefault();
                    newIndex = (currentIndex + 1) % items.length;
                    break;
                case 'ArrowUp':
                    e.preventDefault();
                    newIndex = (currentIndex - 1 + items.length) % items.length;
                    break;
                case 'Home':
                    e.preventDefault();
                    newIndex = 0;
                    break;
                case 'End':
                    e.preventDefault();
                    newIndex = items.length - 1;
                    break;
                default:
                    return;
            }
            items[newIndex]?.focusHeader();
        };
    }
    connectedCallback() {
        super.connectedCallback();
        this.addEventListener('keydown', this._handleKeyDown);
        this.addEventListener('accordion-item-toggle', this._handleItemToggle);
    }
    disconnectedCallback() {
        super.disconnectedCallback();
        this.removeEventListener('keydown', this._handleKeyDown);
        this.removeEventListener('accordion-item-toggle', this._handleItemToggle);
    }
    updated(changedProperties) {
        super.updated(changedProperties);
        if (changedProperties.has('cuePosition') ||
            changedProperties.has('cueType') ||
            changedProperties.has('titleSize') ||
            changedProperties.has('indent') ||
            changedProperties.has('density')) {
            this._propagateToItems();
        }
    }
    _getItems() {
        const slot = this.shadowRoot?.querySelector('slot');
        if (!slot)
            return [];
        return slot
            .assignedElements({ flatten: true })
            .filter((el) => el.tagName === 'MMS-ACCORDION-ITEM');
    }
    _propagateToItems() {
        const items = this._getItems();
        this._items = items;
        items.forEach((item) => {
            item.setAttribute('cue-position', this.cuePosition);
            item.setAttribute('cue-type', this.cueType);
            item.setAttribute('title-size', this.titleSize);
            item.setAttribute('data-density', this.density);
            if (this.indent) {
                item.setAttribute('indent', '');
            }
            else {
                item.removeAttribute('indent');
            }
        });
    }
    render() {
        return html `<slot @slotchange=${this._handleSlotChange}></slot>`;
    }
};
MmsAccordion.styles = css `
    :host {
      display: block;
      box-sizing: border-box;
      width: 100%;
    }
  `;
__decorate([
    property({ type: String, reflect: true })
], MmsAccordion.prototype, "mode", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'cue-position' })
], MmsAccordion.prototype, "cuePosition", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'cue-type' })
], MmsAccordion.prototype, "cueType", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'title-size' })
], MmsAccordion.prototype, "titleSize", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsAccordion.prototype, "indent", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'data-density' })
], MmsAccordion.prototype, "density", void 0);
__decorate([
    state()
], MmsAccordion.prototype, "_items", void 0);
MmsAccordion = __decorate([
    customElement('mms-accordion')
], MmsAccordion);
export { MmsAccordion };
//# sourceMappingURL=mms-accordion.component.js.map