import { LitElement } from 'lit';
import type { PropertyValues } from 'lit';
import '../icon/mms-icon.component.js';
export type StatusTagAlertType = 'success' | 'error' | 'caution' | 'informational';
/**
 * Represents a semantic status (success, error, caution, informational). Not interactive.
 *
 * @element mms-status-tag
 *
 * @prop {StatusTagAlertType} alertType - Semantic type: success (default), error, caution, informational. `warning` is accepted as an alias for `caution`; any other value falls back to success.
 * @prop {boolean} leftIcon - Show the alertType's default icon. Default true.
 * @prop {string} label - Tag label text
 */
export declare class MmsStatusTag extends LitElement {
    alertType: StatusTagAlertType;
    leftIcon: boolean;
    label: string;
    static styles: import("lit").CSSResult;
    /**
     * `alertType` is typed as an enum but arrives as an unvalidated string attribute at runtime.
     * An unrecognized value would miss DEFAULT_LEFT_ICON and pass `undefined` to mms-icon (which
     * then renders its unknown-name placeholder), and would match none of the
     * `:host([alert-type=…])` style blocks, silently falling back to the informational-looking
     * base styling. Normalizing the property clamps both at once — it reflects, so the attribute,
     * the icon and the CSS all agree. Mirrors the `switch`/`default:` guard already used by
     * mms-alert-banner, mms-inline-alert and mms-toast.
     *
     * Aliases resolve first: `warning` is the icon's name and a common guess for this state,
     * so it maps to `caution` rather than being clamped away to the fallback.
     */
    protected willUpdate(changed: PropertyValues<this>): void;
    render(): import("lit-html").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        'mms-status-tag': MmsStatusTag;
    }
}
//# sourceMappingURL=mms-status-tag.component.d.ts.map