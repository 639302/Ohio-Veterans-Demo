var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import { normalizeAlertType } from '../../utils/normalize-alert-type.js';
import '../icon/mms-icon.component.js';
const DEFAULT_LEFT_ICON = {
    success: 'check',
    error: 'x-circle',
    caution: 'warning',
    informational: 'info',
};
const FALLBACK_ALERT_TYPE = 'success';
/**
 * Represents a semantic status (success, error, caution, informational). Not interactive.
 *
 * @element mms-status-tag
 *
 * @prop {StatusTagAlertType} alertType - Semantic type: success (default), error, caution, informational. `warning` is accepted as an alias for `caution`; any other value falls back to success.
 * @prop {boolean} leftIcon - Show the alertType's default icon. Default true.
 * @prop {string} label - Tag label text
 */
let MmsStatusTag = class MmsStatusTag extends LitElement {
    constructor() {
        super(...arguments);
        this.alertType = 'success';
        this.leftIcon = true;
        this.label = '';
    }
    /**
     * `alertType` is typed as an enum but arrives as an unvalidated string attribute at runtime.
     * An unrecognized value would miss DEFAULT_LEFT_ICON and pass `undefined` to mms-icon (which
     * then renders its unknown-name placeholder), and would match none of the
     * `:host([alert-type=…])` style blocks, silently falling back to the informational-looking
     * base styling. Normalizing the property clamps both at once — it reflects, so the attribute,
     * the icon and the CSS all agree. Mirrors the `switch`/`default:` guard already used by
     * mms-alert-banner, mms-inline-alert and mms-toast.
     *
     * Aliases resolve first: `warning` is the icon's name and a common guess for this state,
     * so it maps to `caution` rather than being clamped away to the fallback.
     */
    willUpdate(changed) {
        if (!changed.has('alertType'))
            return;
        const normalized = normalizeAlertType(this.alertType, FALLBACK_ALERT_TYPE);
        if (normalized !== this.alertType)
            this.alertType = normalized;
    }
    render() {
        return html `
      ${this.leftIcon
            ? html `<span class="icon"><mms-icon name=${DEFAULT_LEFT_ICON[this.alertType]} size="sm" weight="regular"></mms-icon></span>`
            : nothing}
      <span class="label">${this.label}</span>
    `;
    }
};
MmsStatusTag.styles = css `
    /* data-density is intentionally not implemented here — mms-status-tag is a static,
       non-interactive label with no touch-target constraint, unlike mms-button's
       compact override. See .claude/contracts/global-props.md. */
    :host {
      --_stroke: var(--color-utility-informational-stroke-fixed);
      --_text: var(--color-text-on-muted);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: var(--spacing-xs2);
      padding: 4px var(--spacing-sm2);
      border-radius: var(--radius-full);
      box-sizing: border-box;
      font-family: var(--type-body-sm-family);
      font-size: var(--type-body-sm-size);
      font-weight: var(--type-body-sm-weight);
      line-height: var(--type-body-sm-line-height);
      border: var(--border-width-xs) solid var(--_stroke);
      background: var(--color-utility-informational-muted-fixed);
      color: var(--_text);
    }

    :host([alert-type='success']) {
      --_stroke: var(--color-utility-success-stroke);
      --_text: var(--color-text-on-muted);
      background: var(--color-utility-success-muted);
    }

    :host([alert-type='error']) {
      --_stroke: var(--color-utility-error-stroke);
      --_text: var(--color-text-on-muted);
      background: var(--color-utility-error-muted);
    }

    :host([alert-type='caution']) {
      --_stroke: var(--color-utility-caution-stroke);
      --_text: var(--color-text-on-muted);
      background: var(--color-utility-caution-muted);
    }

    .icon {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 16px;
      height: 16px;
      flex-shrink: 0;
      color: inherit;
    }

    /* Scale icon visual to 14x14 (matches label font-size) while keeping the 16px hitbox */
    .icon mms-icon {
      transform: scale(0.875);
    }

    .label {
      white-space: nowrap;
    }
  `;
__decorate([
    property({ type: String, reflect: true, attribute: 'alert-type' })
], MmsStatusTag.prototype, "alertType", void 0);
__decorate([
    property({ type: Boolean, attribute: 'left-icon' })
], MmsStatusTag.prototype, "leftIcon", void 0);
__decorate([
    property({ type: String })
], MmsStatusTag.prototype, "label", void 0);
MmsStatusTag = __decorate([
    customElement('mms-status-tag')
], MmsStatusTag);
export { MmsStatusTag };
//# sourceMappingURL=mms-status-tag.component.js.map