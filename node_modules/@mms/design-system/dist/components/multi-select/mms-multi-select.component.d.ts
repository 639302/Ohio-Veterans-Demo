import { LitElement, PropertyValues } from 'lit';
import '../icon/mms-icon.component.js';
import '../tooltip/mms-tooltip.component.js';
export interface MultiSelectOption {
    value: string;
    label: string;
}
export type MultiSelectSize = 'sm' | 'md' | 'lg';
export type MultiSelectState = 'default' | 'hover' | 'focus' | 'filled';
/**
 * `detail` payload of the `change` event. Two shapes, discriminated by which
 * key is present: toggling a single option carries the `option` that changed;
 * the select-all control carries `selectAll` instead. Narrow with `'option' in detail`.
 */
export type MultiSelectChangeDetail = {
    value: string[];
    oldValue: string[];
    option: MultiSelectOption;
} | {
    value: string[];
    oldValue: string[];
    selectAll: boolean;
};
/**
 * mms-multi-select — custom combobox/listbox for selecting multiple options.
 * Marina reference: select-multiple.component.ts (Select Multiple).
 */
export declare class MmsMultiSelect extends LitElement {
    static styles: import("lit").CSSResult[];
    label: string;
    placeholder: string;
    options: MultiSelectOption[];
    value: string[];
    name: string;
    size: MultiSelectSize;
    state: MultiSelectState;
    disabled: boolean;
    readonly: boolean;
    error: boolean;
    errorText: string;
    required: boolean;
    helperText: string;
    showTooltip: boolean;
    tooltipText: string;
    searchable: boolean;
    searchPlaceholder: string;
    showSelectAll: boolean;
    selectAllLabel: string;
    selectedAtTop: boolean;
    emptySearchText: string;
    isOpen: boolean;
    private searchQuery;
    private _boundClickOutside;
    disconnectedCallback(): void;
    get filteredOptions(): MultiSelectOption[];
    get showSelectAllRow(): boolean;
    get allSelected(): boolean;
    get someSelected(): boolean;
    get displayText(): string;
    get isPlaceholder(): boolean;
    get ariaDescribedBy(): string;
    isSelected(value: string): boolean;
    getSelectionState(value: string): 'checked' | 'unchecked';
    getSelectAllState(): 'checked' | 'indeterminate' | 'unchecked';
    private _handleTriggerClick;
    private _focusFirstPanelRow;
    private _handleTriggerKeydown;
    private _handleClickOutside;
    /**
     * The trigger's own native `focus`/`blur` are `composed: true` and would cross
     * the shadow boundary, reaching a host listener alongside the CustomEvents
     * dispatched from `_handleFocusIn`/`_handleFocusOut` — firing a consumer's
     * `onFocus`/`onBlur` twice, with `e.detail` being the number 0 (UIEvent.detail)
     * on the native one. The declared `@fires focus` / `@fires blur` contract is the
     * CustomEvent; the native duplicate is leakage.
     *
     * Only `focus`/`blur` are stopped. `focusin`/`focusout` still propagate out, as
     * they do from any ordinary element — a parent form or focus-tracking library
     * relies on that, and neither name collides with a declared event.
     */
    private _stopNativeFocusEvent;
    private _handleFocusIn;
    private _handleFocusOut;
    private _handleMouseEnter;
    private _handleMouseLeave;
    private _handleOptionClick;
    private _handleSelectAll;
    private _handleSelectAllKeydown;
    private _handleSearchInput;
    private _handleSearchKeydown;
    private _handleClearSearch;
    private _handleOptionKeydown;
    protected firstUpdated(changed: PropertyValues): void;
    render(): import("lit-html").TemplateResult<1>;
}
//# sourceMappingURL=mms-multi-select.component.d.ts.map