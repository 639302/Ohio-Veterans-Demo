var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import { ifDefined } from 'lit/directives/if-defined.js';
import { focusRing } from '../../styles/focus.css.js';
import { validateColorScheme } from '../../utils/validate-color-scheme.js';
import { checkboxIndicatorStyles, renderCheckboxIndicator, } from './checkbox-indicator.parts.js';
/**
 * Checkbox component with label, helper text, and error states.
 *
 * @element mms-checkbox
 *
 * @prop {string} label - Clickable text next to the checkbox indicator
 * @prop {boolean} checked - Whether the checkbox is selected
 * @prop {boolean} indeterminate - Mixed state, typically used for "select all" when some children are checked
 * @prop {CheckboxColorScheme} colorScheme - Brand color applied to checked/indeterminate fill
 * @prop {CheckboxSize} size - Checkbox indicator and text size
 * @prop {CheckboxState} state - Interactive state preview (for documentation)
 * @prop {boolean} readonly - Prevents changes but remains focusable and visible
 * @prop {boolean} disabled - Prevents interaction and dims appearance
 * @prop {boolean} error - Displays error styling and shows error message
 * @prop {string} errorText - Message displayed when error is true
 * @prop {boolean} required - Marks field as required for form validation
 * @prop {string} heading - Optional title displayed above the checkbox
 * @prop {string} helperText - Supplementary guidance displayed below
 * @prop {string} fieldName - Identifier used when submitting form data
 * @prop {string} checkedValue - Value submitted to the form when checked
 *
 * @fires change - When checkbox state changes
 */
let MmsCheckbox = class MmsCheckbox extends LitElement {
    constructor() {
        super(...arguments);
        // ── Core Interaction ────────────────────────────────────────
        this.label = '';
        this.checked = false;
        this.indeterminate = false;
        // ── Visual ──────────────────────────────────────────────────
        this.colorScheme = 'primary';
        this.size = 'md';
        this.state = 'default';
        this.readonly = false;
        this.disabled = false;
        // ── Validation ──────────────────────────────────────────────
        this.error = false;
        this.errorText = '';
        this.required = false;
        // ── Secondary Content ───────────────────────────────────────
        this.heading = '';
        this.helperText = '';
        // ── Form Integration ────────────────────────────────────────
        this.fieldName = '';
        this.checkedValue = 'on';
    }
    // ── Lifecycle ───────────────────────────────────────────────
    firstUpdated() {
        // Validate colorScheme × theme combination
        validateColorScheme(this, this.colorScheme, 'mms-checkbox');
    }
    // ── Event Handlers ──────────────────────────────────────────
    _handleChange(e) {
        if (this.disabled || this.readonly) {
            e.preventDefault();
            return;
        }
        const input = e.target;
        this.checked = input.checked;
        this.indeterminate = false;
        this.dispatchEvent(new CustomEvent('change', {
            detail: { checked: this.checked, value: this.checkedValue },
            bubbles: true,
            composed: true,
        }));
    }
    _handleClick(e) {
        if (this.disabled || this.readonly) {
            e.preventDefault();
        }
    }
    // ── Render ──────────────────────────────────────────────────
    get _ariaDescribedBy() {
        const ids = [];
        if (this.helperText)
            ids.push('helper-text');
        if (this.error && this.errorText)
            ids.push('error-text');
        return ids.length > 0 ? ids.join(' ') : undefined;
    }
    get _indicatorState() {
        if (this.indeterminate)
            return 'indeterminate';
        if (this.checked)
            return 'checked';
        return 'unchecked';
    }
    render() {
        return html `
      ${this.heading
            ? html `<span class="heading" id="heading">${this.heading}${this.required
                ? html `<span class="required-indicator" aria-hidden="true"> *</span>`
                : nothing}</span>`
            : nothing}

      <label class="checkbox-row" @click=${this._handleClick}>
        <span class="indicator-wrapper">
          <input
            type="checkbox"
            class="native-input"
            .checked=${this.checked}
            .indeterminate=${this.indeterminate}
            ?disabled=${this.disabled}
            ?required=${this.required}
            name=${ifDefined(this.fieldName || undefined)}
            value=${this.checkedValue}
            aria-describedby=${ifDefined(this._ariaDescribedBy)}
            aria-invalid=${this.error ? 'true' : 'false'}
            @change=${this._handleChange}
          />
          <span class="hover-bg"></span>
          ${renderCheckboxIndicator(this.size, this._indicatorState, { decorative: false })}
        </span>
        ${this.label
            ? html `<span class="label">${this.label}${this.required && !this.heading
                ? html `<span class="required-indicator" aria-hidden="true"> *</span>`
                : nothing}</span>`
            : nothing}
      </label>

      ${this.helperText ? html `<span class="helper-text" id="helper-text">${this.helperText}</span>` : nothing}
      ${this.error && this.errorText
            ? html `<span class="error-text" id="error-text" role="alert">${this.errorText}</span>`
            : nothing}
    `;
    }
};
MmsCheckbox.styles = [
    focusRing,
    checkboxIndicatorStyles,
    css `
      /* ═══════════════════════════════════════════════════════════════════════
         HOST — Color Scheme Mapping
         ═══════════════════════════════════════════════════════════════════════ */
      :host {
        display: inline-flex;
        flex-direction: column;
        gap: var(--spacing-xs2);
        font-family: var(--type-body-md-family);

        /* Default: primary color scheme */
        --_fill: var(--color-primary-9);
        --_fill-hover: var(--color-primary-10, var(--color-primary-9));
        --_hover-bg: var(--color-primary-2);
        --_stroke: var(--color-border-interactive);
        --_stroke-hover: var(--color-border-interactive-hover);
        --_on-fill: var(--color-text-on-primary);
      }

      :host([color-scheme='secondary']) {
        --_fill: var(--color-secondary-9);
        --_fill-hover: var(--color-secondary-10, var(--color-secondary-9));
        --_hover-bg: var(--color-secondary-2);
        --_on-fill: var(--color-text-on-secondary);
      }

      :host([color-scheme='accent']) {
        --_fill: var(--color-accent-9);
        --_fill-hover: var(--color-accent-10, var(--color-accent-9));
        --_hover-bg: var(--color-accent-2);
        --_on-fill: var(--color-text-on-accent);
      }

      :host([color-scheme='onyx']) {
        --_fill: var(--color-onyx-9);
        --_fill-hover: var(--color-onyx-10, var(--color-onyx-9));
        --_hover-bg: var(--color-onyx-2);
        --_on-fill: var(--color-text-on-onyx);
      }

      /* Mirrors the .checkbox-row min-height/margin-bottom pair below: the
         host element itself (queried directly by mms-checkbox-group's a11y
         tests, from outside the shadow root) must also satisfy the 24px
         WCAG 2.5.8 floor at sm. margin-bottom cancels the growth so stacked
         siblings in a group are unaffected. */
      :host([size='sm']) {
        min-height: var(--size-lg1); /* 24px — WCAG 2.5.8 target size floor */
        margin-bottom: -2px;         /* Offsets the min-height growth; net layout footprint unchanged */
      }

      :host([disabled]) {
        cursor: not-allowed;
        pointer-events: none;
      }

      :host([readonly]) {
        cursor: default;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         HEADING — Optional title above checkbox
         ═══════════════════════════════════════════════════════════════════════ */
      .heading {
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        color: var(--color-text-default);
        margin-bottom: var(--spacing-xs2);
      }

      :host([disabled]) .heading {
        color: var(--color-disabled-text);
      }

      .required-indicator {
        color: var(--color-input-border-error);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         CHECKBOX ROW — Indicator + Label
         ═══════════════════════════════════════════════════════════════════════ */
      .checkbox-row {
        display: inline-flex;
        align-items: flex-start;
        gap: var(--spacing-sm1);
        cursor: pointer;
      }

      /* At sm, the indicator-wrapper's -2px margin-top (below) reduces its
         contribution to this row's flex cross-size below the 24px WCAG 2.5.8
         floor (row measures 22px). min-height restores the floor on the row's
         own box; margin-bottom cancels the resulting growth so layout below
         is unaffected — indicator/label positions and all visible pixels are
         unchanged. */
      :host([size='sm']) .checkbox-row {
        min-height: var(--size-lg1); /* 24px — WCAG 2.5.8 target size floor */
        margin-bottom: -2px;         /* Offsets the min-height growth; net layout footprint unchanged */
      }

      :host([disabled]) .checkbox-row {
        cursor: not-allowed;
        pointer-events: none;
      }

      :host([readonly]) .checkbox-row {
        cursor: default;
        pointer-events: none;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         INDICATOR WRAPPER — Contains touch target + visual indicator
         Marina pattern: indicator flush-left, hover-bg bleeds RIGHT
         ═══════════════════════════════════════════════════════════════════════ */
      .indicator-wrapper {
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: flex-start;
        flex-shrink: 0;
      }

      /* Size variants for wrapper — width matches indicator, height matches touch target */
      :host([size='sm']) .indicator-wrapper {
        width: var(--size-sm2);   /* 12px indicator */
        height: var(--size-lg1);  /* 24px touch target */
        margin-top: -2px;         /* Pull box up to align with label */
      }

      :host([size='md']) .indicator-wrapper,
      :host(:not([size])) .indicator-wrapper {
        width: var(--size-md1);   /* 16px indicator */
        height: var(--size-lg1);  /* 24px touch target */
        /* No margin needed — 16px/24px naturally aligns */
      }

      :host([size='lg']) .indicator-wrapper {
        width: var(--size-md2);   /* 20px indicator */
        height: var(--size-lg2);  /* 32px touch target */
        margin-top: -2px;         /* Pull box up to align with label */
      }

      /* Hidden native input — covers entire touch target */
      .native-input {
        position: absolute;
        inset: 0;
        opacity: 0;
        margin: 0;
        cursor: inherit;
        z-index: 2;
      }

      /* Hover background — centered on indicator */
      .hover-bg {
        position: absolute;
        top: 0;
        bottom: 0;
        border-radius: var(--radius-sm);
        background: transparent;
        transition: background var(--motion-duration-fast) var(--motion-easing-standard);
      }

      :host([size='sm']) .hover-bg {
        width: var(--size-lg1);   /* 24px */
        left: -6px;               /* (24-12)/2 = 6px offset to center */
      }

      :host([size='md']) .hover-bg,
      :host(:not([size])) .hover-bg {
        width: var(--size-lg1);   /* 24px */
        left: -4px;               /* (24-16)/2 = 4px offset to center */
      }

      :host([size='lg']) .hover-bg {
        width: var(--size-lg2);   /* 32px */
        left: -6px;               /* (32-20)/2 = 6px offset to center */
      }

      :host(:not([disabled]):not([readonly])) .checkbox-row:hover .hover-bg,
      :host([state='hover']:not([disabled]):not([readonly])) .hover-bg {
        background: var(--_hover-bg);
      }

      :host([state='focus']) .hover-bg {
        background: transparent;
      }

      /* Base box shape, size variants, checkmark/dash positioning, and
         checked/indeterminate paint live in the shared checkbox-indicator.parts.ts
         partial (also consumed by mms-multi-select's decorative option glyph).
         Only checkbox-specific interactive/validation overrides stay here. */

      :host([checked]:not([disabled]):not([readonly])) .checkbox-row:hover .indicator,
      :host([checked][state='hover']:not([disabled]):not([readonly])) .indicator {
        background: var(--_fill-hover);
        border-color: var(--_fill-hover);
      }

      :host([indeterminate]:not([disabled]):not([readonly])) .checkbox-row:hover .indicator,
      :host([indeterminate][state='hover']:not([disabled]):not([readonly])) .indicator {
        background: var(--_fill-hover);
        border-color: var(--_fill-hover);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         FOCUS STATE
         ═══════════════════════════════════════════════════════════════════════ */
      .native-input:focus-visible ~ .indicator,
      :host([state='focus']) .indicator {
        outline: var(--focus-ring-width) var(--focus-ring-style) var(--focus-ring-color);
        outline-offset: var(--focus-ring-offset);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         DISABLED STATE
         ═══════════════════════════════════════════════════════════════════════ */
      :host([disabled]) .indicator {
        border-color: var(--color-disabled-stroke);
        background: transparent;
      }

      :host([disabled][checked]) .indicator,
      :host([disabled][indeterminate]) .indicator {
        background: var(--color-disabled-solid);
        border-color: var(--color-disabled-solid);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         READONLY STATE
         ═══════════════════════════════════════════════════════════════════════ */
      :host([readonly]) .indicator {
        border-color: var(--color-disabled-stroke);
        background: transparent;
      }

      :host([readonly][checked]) .indicator,
      :host([readonly][indeterminate]) .indicator {
        background: transparent;
        border-color: var(--color-disabled-stroke);
      }

      /* Read-only checkmark uses text color (adapts to light/dark mode) */
      :host([readonly][checked]) .indicator::after {
        border-color: var(--color-text-default);
      }

      /* Read-only indeterminate dash uses text color */
      :host([readonly][indeterminate]) .indicator::before {
        background: var(--color-text-default);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         ERROR STATE
         ═══════════════════════════════════════════════════════════════════════ */
      :host([error]) .indicator {
        border-color: var(--color-input-border-error);
      }

      :host([error][checked]) .indicator,
      :host([error][indeterminate]) .indicator {
        background: var(--color-input-border-error);
        border-color: var(--color-input-border-error);
      }

      /* Error indicator uses on-fill token which swaps in dark mode */
      :host([error][checked]),
      :host([error][indeterminate]) {
        --_on-fill: var(--color-utility-error-on-fill);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         LABEL TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .label {
        color: var(--color-text-default);
        user-select: none;
      }

      :host([size='sm']) .label {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
      }

      :host([size='md']) .label,
      :host(:not([size])) .label {
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
      }

      :host([size='lg']) .label {
        font-size: var(--type-body-lg-size);
        line-height: var(--type-body-lg-line-height);
      }

      :host([disabled]) .label {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         HELPER TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .helper-text {
        font-size: var(--type-ui-caption-size);
        line-height: var(--type-ui-caption-line-height);
        color: var(--color-text-subtle);
      }

      :host([disabled]) .helper-text {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         ERROR TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .error-text {
        font-size: var(--type-ui-caption-size);
        line-height: var(--type-ui-caption-line-height);
        color: var(--color-input-border-error);
      }
    `,
];
__decorate([
    property({ type: String })
], MmsCheckbox.prototype, "label", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckbox.prototype, "checked", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckbox.prototype, "indeterminate", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'color-scheme' })
], MmsCheckbox.prototype, "colorScheme", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsCheckbox.prototype, "size", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsCheckbox.prototype, "state", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckbox.prototype, "readonly", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckbox.prototype, "disabled", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckbox.prototype, "error", void 0);
__decorate([
    property({ type: String, attribute: 'error-text' })
], MmsCheckbox.prototype, "errorText", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsCheckbox.prototype, "required", void 0);
__decorate([
    property({ type: String })
], MmsCheckbox.prototype, "heading", void 0);
__decorate([
    property({ type: String, attribute: 'helper-text' })
], MmsCheckbox.prototype, "helperText", void 0);
__decorate([
    property({ type: String, attribute: 'field-name' })
], MmsCheckbox.prototype, "fieldName", void 0);
__decorate([
    property({ type: String, attribute: 'checked-value' })
], MmsCheckbox.prototype, "checkedValue", void 0);
MmsCheckbox = __decorate([
    customElement('mms-checkbox')
], MmsCheckbox);
export { MmsCheckbox };
// Ensure registration for production builds (Vite tree-shaking safeguard)
if (!customElements.get('mms-checkbox')) {
    customElements.define('mms-checkbox', MmsCheckbox);
}
//# sourceMappingURL=mms-checkbox.component.js.map