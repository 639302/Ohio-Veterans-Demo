export type CheckboxIndicatorSize = 'sm' | 'md' | 'lg';
export type CheckboxIndicatorState = 'unchecked' | 'checked' | 'indeterminate';
/**
 * The visual checkbox glyph shared by mms-checkbox (interactive, wraps a native
 * input) and mms-multi-select (decorative, aria-hidden — selection state lives
 * on the option's aria-selected). State is carried via data-attributes rather
 * than :host(...) so the same markup/CSS works regardless of which component's
 * shadow root it renders in.
 */
export declare function renderCheckboxIndicator(size: CheckboxIndicatorSize, state: CheckboxIndicatorState, options?: {
    decorative?: boolean;
}): import("lit-html").TemplateResult<1>;
export declare const checkboxIndicatorStyles: import("lit").CSSResult;
//# sourceMappingURL=checkbox-indicator.parts.d.ts.map