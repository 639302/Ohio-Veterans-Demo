import { css, html, nothing } from 'lit';
/**
 * The visual checkbox glyph shared by mms-checkbox (interactive, wraps a native
 * input) and mms-multi-select (decorative, aria-hidden — selection state lives
 * on the option's aria-selected). State is carried via data-attributes rather
 * than :host(...) so the same markup/CSS works regardless of which component's
 * shadow root it renders in.
 */
export function renderCheckboxIndicator(size, state, options = {}) {
    return html `<span
    class="indicator"
    data-size=${size}
    data-state=${state}
    aria-hidden=${options.decorative === false ? nothing : 'true'}
  ></span>`;
}
export const checkboxIndicatorStyles = css `
  .indicator {
    position: relative;
    box-sizing: border-box;
    flex-shrink: 0;
    border: var(--border-width-xs) solid var(--_stroke, var(--color-border-interactive));
    border-radius: var(--radius-xs);
    background: transparent;
    transition:
      background var(--motion-duration-fast) var(--motion-easing-standard),
      border-color var(--motion-duration-fast) var(--motion-easing-standard);
    z-index: 1;
  }

  .indicator[data-size='sm'] {
    width: var(--size-sm2); /* 12px */
    height: var(--size-sm2);
  }

  .indicator[data-size='md'] {
    width: var(--size-md1); /* 16px */
    height: var(--size-md1);
  }

  .indicator[data-size='lg'] {
    width: var(--size-md2); /* 20px */
    height: var(--size-md2);
  }

  /* Checkmark (checked) and dash (indeterminate) — hidden by default */
  .indicator::after {
    content: '';
    position: absolute;
    display: none;
    border: solid var(--_on-fill, var(--color-text-on-onyx));
    border-width: 0 2px 2px 0;
    transform: rotate(45deg);
  }

  .indicator::before {
    content: '';
    position: absolute;
    display: none;
    height: 2px;
    background: var(--_on-fill, var(--color-text-on-onyx));
  }

  /* Checkmark: 3px x 6px at (3px, 0px) — dash: 8px wide, centered */
  .indicator[data-size='sm']::after {
    left: 3px;
    top: 0px;
    width: 3px;
    height: 6px;
  }
  .indicator[data-size='sm']::before {
    left: 1px;
    top: 4px;
    width: 8px;
  }

  /* Checkmark: 4px x 8px at (4px, 0px) — dash: 10px wide, centered */
  .indicator[data-size='md']::after {
    left: 4px;
    top: 0px;
    width: 4px;
    height: 8px;
  }
  .indicator[data-size='md']::before {
    left: 2px;
    top: 6px;
    width: 10px;
  }

  /* Checkmark: 5px x 10px at (6px, 1px) — dash: 12px wide, centered */
  .indicator[data-size='lg']::after {
    left: 6px;
    top: 1px;
    width: 5px;
    height: 10px;
  }
  .indicator[data-size='lg']::before {
    left: 3px;
    top: 8px;
    width: 12px;
  }

  .indicator[data-state='checked'],
  .indicator[data-state='indeterminate'] {
    background: var(--_fill, var(--color-onyx-9));
    border-color: var(--_fill, var(--color-onyx-9));
  }

  .indicator[data-state='checked']::after {
    display: block;
  }

  .indicator[data-state='indeterminate']::before {
    display: block;
  }
`;
//# sourceMappingURL=checkbox-indicator.parts.js.map