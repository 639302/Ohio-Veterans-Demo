import { LitElement } from 'lit';
import type { PropertyValues } from 'lit';
import '../icon/mms-icon.component.js';
import '../link/mms-link.component.js';
export type InlineAlertType = 'informational' | 'error' | 'success' | 'caution';
/** `detail` payload of the `action-click` event. */
export type InlineAlertActionClickDetail = {
    href: string;
};
/**
 * Inline Alert component for contextual messages associated with a section,
 * form field, or item on a page (as opposed to mms-alert-banner's page-level
 * placement).
 *
 * @element mms-inline-alert
 *
 * @prop {InlineAlertType} alertType - Visual/semantic type: informational (default), error, success, caution. `warning` is accepted as an alias for `caution`; any other value falls back to informational.
 * @prop {boolean} dismissible - Show the close button. Default true.
 * @prop {boolean} action - Show the text link action. Default false.
 * @prop {string} summary - Bold summary text rendered before the message
 * @prop {string} message - Regular message text
 * @prop {string} actionText - Text link label
 * @prop {string} actionHref - Text link URL
 *
 * @fires dismiss - Fired when the close button is clicked
 * @fires action-click - Fired when the text link is clicked. `detail: { href: string }`
 */
export declare class MmsInlineAlert extends LitElement {
    alertType: InlineAlertType;
    dismissible: boolean;
    action: boolean;
    summary: string;
    message: string;
    actionText: string;
    actionHref: string;
    static styles: import("lit").CSSResult[];
    private _getIconName;
    private _handleDismiss;
    private _handleActionClick;
    connectedCallback(): void;
    /**
     * Resolves the `warning` → `caution` alias, then clamps anything still outside the
     * enum back to the property default. Both matter: an unrecognised `alert-type`
     * matches no `:host([alert-type=…])` block and falls through the icon `switch`, so
     * without the clamp the component renders an informational alert where the author
     * asked for an error one — wrong status, no warning. See utils/normalize-alert-type.
     */
    protected willUpdate(changed: PropertyValues<this>): void;
    render(): import("lit-html").TemplateResult<1>;
}
declare global {
    interface HTMLElementTagNameMap {
        'mms-inline-alert': MmsInlineAlert;
    }
}
//# sourceMappingURL=mms-inline-alert.component.d.ts.map