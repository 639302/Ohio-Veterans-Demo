var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property, state } from 'lit/decorators.js';
import { focusRing } from '../../styles/focus.css.js';
import '../icon/mms-icon.component.js';
import '../tooltip/mms-tooltip.component.js';
/**
 * Text field component for single-line text input.
 *
 * Uses native `<input>` element for built-in accessibility and keyboard navigation.
 * Provides label, helper text, validation, masks, and integrates with forms.
 *
 * @element mms-text-field
 *
 * @prop {string} label - Label text (if empty, label row hidden)
 * @prop {string} value - Current input value
 * @prop {string} placeholder - Placeholder shown when no value
 * @prop {string} helperText - Guidance text below input
 * @prop {string} prefixIcon - Icon name for left side
 * @prop {string} suffixIcon - Icon name for right side
 * @prop {boolean} clearButton - Shows clear (×) button when has value
 * @prop {MaskType} mask - Input formatting mask
 * @prop {PhoneLocale} phoneLocale - Country code for phone mask
 * @prop {CurrencyCode} currency - Currency symbol for currency mask
 * @prop {string} measurementUnit - Unit suffix for measurement mask
 * @prop {string} name - Form field name for submission
 * @prop {InputType} inputType - HTML input type
 * @prop {string} autocomplete - Browser autocomplete hint
 * @prop {boolean} required - Shows asterisk indicator on label
 * @prop {boolean} error - Displays error styling and error message
 * @prop {string} errorText - Error message when error is true
 * @prop {number} maxLength - Character limit (0 = unlimited)
 * @prop {boolean} showCharacterCounter - Displays remaining chars
 * @prop {boolean} showTooltip - Shows info icon next to label
 * @prop {string} tooltipText - Tooltip content
 * @prop {TextFieldSize} size - Text/padding size (sm, md, lg)
 * @prop {TextFieldState} state - Preview state for Storybook demos
 * @prop {boolean} disabled - Prevents interaction, dims appearance
 * @prop {boolean} readonly - Shows value but prevents changes
 *
 * @fires input - On every keystroke, detail: { value, oldValue }
 * @fires change - On blur after change, detail: { value }
 * @fires focus - When input receives focus
 * @fires blur - When input loses focus
 * @fires clear - When clear button clicked
 */
let MmsTextField = class MmsTextField extends LitElement {
    constructor() {
        super(...arguments);
        // ── Visual ──────────────────────────────────────────────────
        this.size = 'md';
        this.state = 'default';
        this.disabled = false;
        this.readonly = false;
        // ── Content ─────────────────────────────────────────────────
        this.label = '';
        this.placeholder = '';
        this.helperText = '';
        this.prefixIcon = '';
        this.suffixIcon = '';
        this.clearButton = false;
        // ── Mask ────────────────────────────────────────────────────
        this.mask = 'none';
        this.phoneLocale = 'US';
        this.currency = 'USD';
        this.measurementUnit = '';
        // ── Form ────────────────────────────────────────────────────
        this.name = '';
        this.value = '';
        this.inputType = 'text';
        this.autocomplete = '';
        // ── Validation ──────────────────────────────────────────────
        this.required = false;
        this.error = false;
        this.errorText = '';
        this.maxLength = 0;
        this.showCharacterCounter = false;
        // ── Tooltip ─────────────────────────────────────────────────
        this.showTooltip = false;
        this.tooltipText = '';
        // ── Internal State ──────────────────────────────────────────
        this._showPassword = false;
    }
    // ── Computed ────────────────────────────────────────────────
    /** Icon size scales with component size */
    get iconSize() {
        switch (this.size) {
            case 'sm':
                return 'sm'; // 16px
            case 'lg':
                return 'md'; // 20px
            default:
                return 'sm'; // 16px
        }
    }
    /** Effective input type based on mask and password visibility */
    get effectiveInputType() {
        if (this.mask === 'protected-field') {
            return this._showPassword ? 'text' : 'password';
        }
        if (this.mask === 'email')
            return 'email';
        if (this.mask === 'phone-number')
            return 'tel';
        return this.inputType;
    }
    /** Effective autocomplete based on mask */
    get effectiveAutocomplete() {
        if (this.autocomplete)
            return this.autocomplete;
        switch (this.mask) {
            case 'email':
                return 'email';
            case 'phone-number':
                return 'tel';
            case 'protected-field':
                return 'new-password';
            default:
                return 'off';
        }
    }
    /** Phone country code prefix */
    get phoneCountryCode() {
        if (this.mask !== 'phone-number')
            return '';
        const codes = {
            US: '+1',
            MX: '+52',
            UK: '+44',
            FR: '+33',
            SG: '+65',
            HK: '+852',
            AU: '+61',
            TH: '+66',
            MY: '+60',
            GR: '+30',
            IN: '+91',
            JP: '+81',
            KR: '+82',
        };
        return codes[this.phoneLocale] || '+1';
    }
    /** Currency symbol prefix */
    get currencySymbol() {
        if (this.mask !== 'currency')
            return '';
        const symbols = {
            USD: '$',
            EUR: '€',
            GBP: '£',
            JPY: '¥',
            CNY: '¥',
            AUD: '$',
            CAD: '$',
            CHF: 'CHF',
            HKD: '$',
            SGD: '$',
            INR: '₹',
            KRW: '₩',
            MXN: '$',
        };
        return symbols[this.currency] || '$';
    }
    /** Effective prefix icon (mask can override) */
    get effectivePrefixIcon() {
        if (this.mask === 'email')
            return 'envelope';
        return this.prefixIcon;
    }
    /** Effective placeholder based on mask (auto-generates when empty) */
    get effectivePlaceholder() {
        if (this.placeholder)
            return this.placeholder;
        // Auto-generate mask-specific placeholders
        switch (this.mask) {
            case 'email':
                return 'name@example.com';
            case 'phone-number':
                switch (this.phoneLocale) {
                    case 'US': return '(000) 000-0000';
                    case 'MX': return '000-000-0000';
                    case 'UK': return '00000-000000';
                    case 'FR': return '00-00-00-00-00';
                    case 'SG': return '0000-0000';
                    case 'HK': return '0000-0000';
                    case 'AU': return '0000-000-000';
                    case 'TH': return '000-000-0000';
                    case 'MY': return '00-0000-0000';
                    case 'GR': return '000-000-0000';
                    case 'IN': return '00000-00000';
                    case 'JP': return '000-0000-0000';
                    case 'KR': return '000-0000-0000';
                    default: return '(000) 000-0000';
                }
            case 'currency':
                return '0.00';
            case 'numeral':
            case 'measurement':
            case 'count':
                return '0';
            case 'protected-field':
                return 'Enter password';
            default:
                return '';
        }
    }
    /** Check if clear button should be shown */
    get showClearButton() {
        return (this.clearButton &&
            this.value.length > 0 &&
            !this.disabled &&
            !this.readonly);
    }
    /** ARIA describedby IDs */
    get ariaDescribedBy() {
        const ids = [];
        if (this.helperText && !this.error)
            ids.push('helper-text');
        if (this.error && this.errorText)
            ids.push('error-text');
        if (this.showCharacterCounter && this.maxLength > 0)
            ids.push('char-counter');
        return ids.join(' ') || undefined;
    }
    /** Characters remaining */
    get charsRemaining() {
        return this.maxLength - this.value.length;
    }
    // ── Event Handlers ────────────────────────────────────────────
    _handleInput(e) {
        // The inner <input>'s native `input` event is `composed: true`, so without
        // this it crosses the shadow boundary and reaches a host listener alongside
        // the CustomEvent dispatched below — firing `onInput` twice per keystroke,
        // with `e.detail` being the number 0 (UIEvent.detail) on the native one. The
        // component's declared `@fires input` contract is the CustomEvent; the native
        // duplicate is leakage. Stopped before the guard clause below so the contract
        // holds unconditionally, not just on the path that re-dispatches.
        e.stopPropagation();
        if (this.disabled || this.readonly)
            return;
        const input = e.target;
        const oldValue = this.value;
        let newValue = input.value;
        // Apply formatting based on mask
        newValue = this._formatValue(newValue);
        // Enforce maxLength if set
        if (this.maxLength > 0 && newValue.length > this.maxLength) {
            newValue = newValue.slice(0, this.maxLength);
        }
        this.value = newValue;
        // Update input if formatting changed the value
        if (input.value !== newValue) {
            input.value = newValue;
        }
        this.dispatchEvent(new CustomEvent('input', {
            detail: { value: this.value, oldValue },
            bubbles: true,
            composed: true,
        }));
    }
    _handleFocus(e) {
        // Native focus/blur are composed too — same leakage as _handleInput above.
        e.stopPropagation();
        if (this.disabled)
            return;
        this.state = 'focus';
        this.dispatchEvent(new CustomEvent('focus', {
            detail: { value: this.value },
            bubbles: true,
            composed: true,
        }));
    }
    _handleBlur(e) {
        e.stopPropagation();
        if (this.disabled)
            return;
        this.state = this.value ? 'filled' : 'default';
        this.dispatchEvent(new CustomEvent('blur', {
            detail: { value: this.value },
            bubbles: true,
            composed: true,
        }));
        this.dispatchEvent(new CustomEvent('change', {
            detail: { value: this.value },
            bubbles: true,
            composed: true,
        }));
    }
    _handleClear() {
        if (this.disabled || this.readonly)
            return;
        this.value = '';
        this.state = 'default';
        this.dispatchEvent(new CustomEvent('clear', {
            bubbles: true,
            composed: true,
        }));
        this.dispatchEvent(new CustomEvent('input', {
            detail: { value: '', oldValue: this.value },
            bubbles: true,
            composed: true,
        }));
        // Focus the input after clearing
        const input = this.shadowRoot?.querySelector('.input-field');
        input?.focus();
    }
    _handleTogglePassword() {
        if (this.disabled || this.readonly)
            return;
        this._showPassword = !this._showPassword;
    }
    // ── Formatting ────────────────────────────────────────────────
    _formatValue(value) {
        switch (this.mask) {
            case 'phone-number':
                return this._formatPhoneNumber(value);
            case 'numeral':
                return this._formatNumeral(value);
            case 'currency':
                return this._formatCurrency(value);
            case 'measurement':
            case 'count':
                return this._formatInteger(value);
            default:
                return value;
        }
    }
    _formatPhoneNumber(value) {
        const digits = value.replace(/\D/g, '');
        switch (this.phoneLocale) {
            case 'US':
                if (digits.length <= 3)
                    return digits;
                if (digits.length <= 6)
                    return `(${digits.slice(0, 3)}) ${digits.slice(3)}`;
                return `(${digits.slice(0, 3)}) ${digits.slice(3, 6)}-${digits.slice(6, 10)}`;
            case 'UK':
                if (digits.length <= 5)
                    return digits;
                return `${digits.slice(0, 5)}-${digits.slice(5, 11)}`;
            case 'FR':
                if (digits.length <= 2)
                    return digits;
                if (digits.length <= 4)
                    return `${digits.slice(0, 2)}-${digits.slice(2)}`;
                if (digits.length <= 6)
                    return `${digits.slice(0, 2)}-${digits.slice(2, 4)}-${digits.slice(4)}`;
                if (digits.length <= 8)
                    return `${digits.slice(0, 2)}-${digits.slice(2, 4)}-${digits.slice(4, 6)}-${digits.slice(6)}`;
                return `${digits.slice(0, 2)}-${digits.slice(2, 4)}-${digits.slice(4, 6)}-${digits.slice(6, 8)}-${digits.slice(8, 10)}`;
            case 'SG':
            case 'HK':
                if (digits.length <= 4)
                    return digits;
                return `${digits.slice(0, 4)}-${digits.slice(4, 8)}`;
            case 'AU':
                if (digits.length <= 4)
                    return digits;
                if (digits.length <= 7)
                    return `${digits.slice(0, 4)}-${digits.slice(4)}`;
                return `${digits.slice(0, 4)}-${digits.slice(4, 7)}-${digits.slice(7, 10)}`;
            case 'IN':
                if (digits.length <= 5)
                    return digits;
                return `${digits.slice(0, 5)}-${digits.slice(5, 10)}`;
            case 'JP':
            case 'KR':
                if (digits.length <= 3)
                    return digits;
                if (digits.length <= 7)
                    return `${digits.slice(0, 3)}-${digits.slice(3)}`;
                return `${digits.slice(0, 3)}-${digits.slice(3, 7)}-${digits.slice(7, 11)}`;
            default:
                // MX, TH, GR, MY - generic format
                if (digits.length <= 3)
                    return digits;
                if (digits.length <= 6)
                    return `${digits.slice(0, 3)}-${digits.slice(3)}`;
                return `${digits.slice(0, 3)}-${digits.slice(3, 6)}-${digits.slice(6, 10)}`;
        }
    }
    _formatNumeral(value) {
        const cleaned = value.replace(/[^\d.]/g, '');
        const parts = cleaned.split('.');
        const integerPart = parts[0].replace(/\B(?=(\d{3})+(?!\d))/g, ',');
        return parts[1] !== undefined
            ? `${integerPart}.${parts[1]}`
            : integerPart;
    }
    _formatCurrency(value) {
        const cleaned = value.replace(/[^\d.]/g, '');
        if (!cleaned)
            return '';
        const parts = cleaned.split('.');
        const integerPart = parts[0].replace(/\B(?=(\d{3})+(?!\d))/g, ',');
        let decimalPart = parts[1];
        if (decimalPart !== undefined) {
            decimalPart = decimalPart.slice(0, 2);
            return `${integerPart}.${decimalPart}`;
        }
        return integerPart;
    }
    _formatInteger(value) {
        const cleaned = value.replace(/\D/g, '');
        if (!cleaned)
            return '';
        return cleaned.replace(/\B(?=(\d{3})+(?!\d))/g, ',');
    }
    // ── Render ────────────────────────────────────────────────────
    render() {
        const hasPrefix = this.effectivePrefixIcon ||
            this.phoneCountryCode ||
            this.currencySymbol ||
            this.mask === 'count';
        const hasSuffix = this.suffixIcon ||
            this.measurementUnit ||
            this.mask === 'protected-field' ||
            this.showClearButton;
        return html `
      ${this.label
            ? html `
            <div class="label-row">
              <span>${this.label}</span>
              ${this.required
                ? html `<span class="required-indicator" aria-hidden="true"
                    >*</span
                  >`
                : nothing}
              ${this.showTooltip
                ? html `
                    <mms-tooltip text="${this.tooltipText}">
                      <mms-icon
                        class="tooltip-icon"
                        name="info"
                        size="sm"
                        aria-label="${this.tooltipText || 'More information'}"
                      ></mms-icon>
                    </mms-tooltip>
                  `
                : nothing}
            </div>
          `
            : nothing}

      <div class="input-container">
        ${this.effectivePrefixIcon
            ? html `
              <mms-icon
                class="prefix-icon"
                name="${this.effectivePrefixIcon}"
                size="${this.iconSize}"
              ></mms-icon>
            `
            : nothing}
        ${this.phoneCountryCode
            ? html `<span class="prefix-text">${this.phoneCountryCode}</span>`
            : nothing}
        ${this.currencySymbol
            ? html `<span class="prefix-text">${this.currencySymbol}</span>`
            : nothing}
        ${this.mask === 'count'
            ? html `<span class="prefix-text">#</span>`
            : nothing}

        <input
          class="input-field ${hasPrefix ? 'has-prefix' : ''} ${hasSuffix
            ? 'has-suffix'
            : ''}"
          type="${this.effectiveInputType}"
          .value="${this.value}"
          placeholder="${this.effectivePlaceholder}"
          name="${this.name || nothing}"
          autocomplete="${this.effectiveAutocomplete}"
          ?disabled="${this.disabled}"
          ?readonly="${this.readonly}"
          ?required="${this.required}"
          aria-label="${this.label || this.effectivePlaceholder}"
          aria-invalid="${this.error}"
          aria-describedby="${this.ariaDescribedBy || nothing}"
          maxlength="${this.maxLength > 0 ? this.maxLength : nothing}"
          @input="${this._handleInput}"
          @focus="${this._handleFocus}"
          @blur="${this._handleBlur}"
        />

        ${this.measurementUnit
            ? html `<span class="suffix-text">${this.measurementUnit}</span>`
            : nothing}
        ${this.suffixIcon && this.mask !== 'protected-field'
            ? html `
              <mms-icon
                class="suffix-icon"
                name="${this.suffixIcon}"
                size="${this.iconSize}"
              ></mms-icon>
            `
            : nothing}
        ${this.mask === 'protected-field'
            ? html `
              <button
                type="button"
                class="password-toggle"
                @click="${this._handleTogglePassword}"
                aria-label="${this._showPassword
                ? 'Hide password'
                : 'Show password'}"
                ?disabled="${this.disabled}"
              >
                <mms-icon
                  name="${this._showPassword ? 'eye' : 'eye-closed'}"
                  size="${this.iconSize}"
                ></mms-icon>
              </button>
            `
            : nothing}
        ${this.showClearButton
            ? html `
              <button
                type="button"
                class="clear-button"
                @click="${this._handleClear}"
                aria-label="Clear input"
              >
                <mms-icon name="x" size="${this.iconSize}"></mms-icon>
              </button>
            `
            : nothing}
      </div>

      ${this.showCharacterCounter && this.maxLength > 0
            ? html `
            <div
              id="char-counter"
              class="char-counter ${this.charsRemaining <= 10
                ? 'warning'
                : ''} ${this.charsRemaining < 0 ? 'error' : ''}"
              aria-live="polite"
            >
              ${this.charsRemaining} characters remaining
            </div>
          `
            : nothing}
      ${this.helperText && !this.error && !this.disabled
            ? html `<span id="helper-text" class="helper-text"
            >${this.helperText}</span
          >`
            : nothing}
      ${this.error && this.errorText && !this.disabled
            ? html `
            <span id="error-text" class="error-text" role="alert">
              <mms-icon class="error-icon" name="warning-circle" size="16"></mms-icon>
              ${this.errorText}
            </span>
          `
            : nothing}
    `;
    }
};
MmsTextField.styles = [
    focusRing,
    css `
      /* ═══════════════════════════════════════════════════════════════════════
         HOST
         ═══════════════════════════════════════════════════════════════════════ */
      :host {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-xs2);
        font-family: var(--type-body-md-family);
      }

      :host([disabled]) {
        cursor: not-allowed;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         LABEL ROW — Label with optional required indicator and tooltip
         ═══════════════════════════════════════════════════════════════════════ */
      .label-row {
        display: flex;
        align-items: center;
        gap: var(--spacing-xs2);
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        color: var(--color-text-default);
      }

      :host([size='sm']) .label-row {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
      }

      :host([size='lg']) .label-row {
        font-size: var(--type-body-lg-size);
        line-height: var(--type-body-lg-line-height);
      }

      :host([disabled]) .label-row {
        color: var(--color-disabled-text);
      }

      .required-indicator {
        color: var(--color-input-border-error);
      }

      .tooltip-icon {
        flex-shrink: 0;
        width: 16px;
        height: 16px;
        color: var(--color-text-subtle);
      }

      :host([disabled]) .tooltip-icon {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         INPUT CONTAINER — Holds input and icons
         ═══════════════════════════════════════════════════════════════════════ */
      .input-container {
        display: flex;
        align-items: center;
        position: relative;
        box-sizing: border-box;
        min-width: 120px;
        background: var(--color-surface-raised);
        border: 1px solid var(--color-neutral-7);
        border-radius: var(--radius-sm);
        transition: border-color 0.15s ease, box-shadow 0.15s ease;
      }

      /* Hover state — inset shadow for visual distinction (no layout shift) */
      :host(:not([disabled]):not([readonly])) .input-container:hover,
      :host([state='hover']:not([disabled]):not([readonly])) .input-container {
        border-color: var(--color-border-interactive-hover);
        box-shadow: inset 0 0 0 1px var(--color-border-interactive-hover);
      }

      /* Focus state */
      :host([state='focus']:not([disabled])) .input-container,
      .input-container:focus-within {
        border-color: var(--focus-ring-color);
        box-shadow: 0 0 0 1px var(--focus-ring-color);
      }

      /* Filled state */
      :host([state='filled']:not([disabled]):not([error])) .input-container {
        border-color: var(--color-neutral-9);
      }

      /* Error state */
      :host([error]) .input-container {
        border-color: var(--color-input-border-error);
      }

      :host([error]:not([disabled]):not([readonly])) .input-container:hover,
      :host([error][state='hover']:not([disabled]):not([readonly])) .input-container {
        border-color: var(--color-input-border-error);
        box-shadow: inset 0 0 0 1px var(--color-input-border-error);
      }

      :host([error][state='focus']) .input-container,
      :host([error]) .input-container:focus-within {
        border-color: var(--focus-ring-color);
        box-shadow: 0 0 0 1px var(--focus-ring-color);
      }

      /* Disabled state */
      :host([disabled]) .input-container {
        background: var(--color-disabled-surface);
        border-color: var(--color-disabled-stroke);
        cursor: not-allowed;
      }

      /* Readonly state */
      :host([readonly]) .input-container {
        background: var(--color-neutral-2);
        border-color: var(--color-neutral-5);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         PREFIX — Icon or text before input
         ═══════════════════════════════════════════════════════════════════════ */
      .prefix-icon,
      .prefix-text {
        flex-shrink: 0;
        margin-left: var(--spacing-sm2);
        color: var(--color-text-subtle);
      }

      :host([disabled]) .prefix-icon,
      :host([disabled]) .prefix-text {
        color: var(--color-disabled-text);
      }

      .prefix-text {
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        white-space: nowrap;
      }

      :host([size='sm']) .prefix-text {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
      }

      :host([size='lg']) .prefix-text {
        font-size: var(--type-body-lg-size);
        line-height: var(--type-body-lg-line-height);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         INPUT FIELD
         ═══════════════════════════════════════════════════════════════════════ */
      .input-field {
        flex: 1;
        min-width: 60px;
        border: none;
        outline: none;
        background: transparent;
        font-family: inherit;
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        color: var(--color-text-default);
        padding: var(--spacing-sm2);
        box-sizing: border-box;
      }

      :host([size='sm']) .input-field {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
        padding: var(--spacing-xs2) var(--spacing-sm1);
      }

      :host([size='lg']) .input-field {
        font-size: var(--type-body-lg-size);
        line-height: var(--type-body-lg-line-height);
        padding: var(--spacing-md1) var(--spacing-sm2);
      }

      /* Remove left padding when prefix present */
      .input-field.has-prefix {
        padding-left: var(--spacing-xs2);
      }

      /* Remove right padding when suffix present */
      .input-field.has-suffix {
        padding-right: var(--spacing-xs2);
      }

      /* Placeholder */
      .input-field::placeholder {
        color: var(--color-text-placeholder);
      }

      :host(:hover:not([disabled]):not([readonly])) .input-field::placeholder {
        color: var(--color-text-default);
      }

      :host([error]) .input-field::placeholder {
        color: var(--color-input-border-error);
        opacity: 0.7;
      }

      /* Disabled */
      :host([disabled]) .input-field {
        color: var(--color-disabled-text);
        cursor: not-allowed;
      }

      :host([disabled]) .input-field::placeholder {
        color: var(--color-disabled-text);
      }

      /* Readonly */
      :host([readonly]) .input-field {
        color: var(--color-text-subtle);
        cursor: default;
      }

      /* Right-align for numeric masks */
      :host([mask='numeral']) .input-field,
      :host([mask='currency']) .input-field,
      :host([mask='measurement']) .input-field,
      :host([mask='count']) .input-field {
        text-align: right;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         SUFFIX — Icons and buttons after input
         ═══════════════════════════════════════════════════════════════════════ */
      .suffix-icon {
        flex-shrink: 0;
        margin-right: var(--spacing-sm2);
        color: var(--color-text-subtle);
        cursor: pointer;
      }

      :host([disabled]) .suffix-icon {
        color: var(--color-disabled-text);
        cursor: not-allowed;
      }

      .suffix-text {
        flex-shrink: 0;
        margin-right: var(--spacing-sm2);
        color: var(--color-text-subtle);
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        white-space: nowrap;
      }

      :host([size='sm']) .suffix-text {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
      }

      :host([size='lg']) .suffix-text {
        font-size: var(--type-body-lg-size);
        line-height: var(--type-body-lg-line-height);
      }

      :host([disabled]) .suffix-text {
        color: var(--color-disabled-text);
      }

      .clear-button,
      .password-toggle {
        flex-shrink: 0;
        margin-right: var(--spacing-sm2);
        color: var(--color-text-subtle);
        cursor: pointer;
        background: none;
        border: none;
        padding: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      /* WCAG 2.2 target-size: 44x44px minimum touch target
         Padding expands touch area, negative margin preserves layout */
      .password-toggle {
        padding: 14px; /* (44 - 16) / 2 = 14px around 16px icon */
        margin: -14px;
        margin-right: calc(var(--spacing-sm2) - 14px); /* Preserve original right spacing */
      }

      :host([size='lg']) .password-toggle {
        padding: 12px; /* (44 - 20) / 2 = 12px around 20px icon */
        margin: -12px;
        margin-right: calc(var(--spacing-sm2) - 12px);
      }

      .clear-button:hover,
      .password-toggle:hover {
        color: var(--color-text-default);
      }

      :host([disabled]) .clear-button,
      :host([disabled]) .password-toggle {
        color: var(--color-disabled-text);
        cursor: not-allowed;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         HELPER TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .helper-text {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
        color: var(--color-text-subtle);
      }

      :host([disabled]) .helper-text {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         ERROR TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .error-text {
        display: flex;
        align-items: flex-start;
        gap: var(--spacing-xs2);
        font-size: var(--type-ui-caption-size);
        line-height: var(--type-ui-caption-line-height);
        color: var(--color-input-border-error);
      }

      .error-icon {
        flex-shrink: 0;
        width: 16px;
        height: 16px;
        color: var(--color-input-border-error);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         CHARACTER COUNTER
         ═══════════════════════════════════════════════════════════════════════ */
      .char-counter {
        font-size: var(--type-body-sm-size);
        line-height: var(--type-body-sm-line-height);
        color: var(--color-text-subtle);
        text-align: right;
      }

      .char-counter.warning {
        color: var(--color-utility-caution-emphasis);
      }

      .char-counter.error {
        color: var(--color-input-border-error);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         DENSITY: COMPACT
         ═══════════════════════════════════════════════════════════════════════ */
      :host([data-density='compact']) {
        gap: var(--spacing-xs1);
      }

      :host([data-density='compact']) .input-field {
        padding: var(--spacing-xs2) var(--spacing-sm1);
      }

      :host([data-density='compact'][size='sm']) .input-field {
        padding: var(--spacing-xs1) var(--spacing-xs2);
      }

      :host([data-density='compact'][size='lg']) .input-field {
        padding: var(--spacing-sm1) var(--spacing-sm2);
      }
    `,
];
__decorate([
    property({ type: String, reflect: true })
], MmsTextField.prototype, "size", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsTextField.prototype, "state", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsTextField.prototype, "disabled", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsTextField.prototype, "readonly", void 0);
__decorate([
    property({ type: String })
], MmsTextField.prototype, "label", void 0);
__decorate([
    property({ type: String })
], MmsTextField.prototype, "placeholder", void 0);
__decorate([
    property({ type: String, attribute: 'helper-text' })
], MmsTextField.prototype, "helperText", void 0);
__decorate([
    property({ type: String, attribute: 'prefix-icon' })
], MmsTextField.prototype, "prefixIcon", void 0);
__decorate([
    property({ type: String, attribute: 'suffix-icon' })
], MmsTextField.prototype, "suffixIcon", void 0);
__decorate([
    property({ type: Boolean, reflect: true, attribute: 'clear-button' })
], MmsTextField.prototype, "clearButton", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsTextField.prototype, "mask", void 0);
__decorate([
    property({ type: String, attribute: 'phone-locale' })
], MmsTextField.prototype, "phoneLocale", void 0);
__decorate([
    property({ type: String })
], MmsTextField.prototype, "currency", void 0);
__decorate([
    property({ type: String, attribute: 'measurement-unit' })
], MmsTextField.prototype, "measurementUnit", void 0);
__decorate([
    property({ type: String })
], MmsTextField.prototype, "name", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsTextField.prototype, "value", void 0);
__decorate([
    property({ type: String, attribute: 'type' })
], MmsTextField.prototype, "inputType", void 0);
__decorate([
    property({ type: String })
], MmsTextField.prototype, "autocomplete", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsTextField.prototype, "required", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsTextField.prototype, "error", void 0);
__decorate([
    property({ type: String, attribute: 'error-text' })
], MmsTextField.prototype, "errorText", void 0);
__decorate([
    property({ type: Number, attribute: 'max-length' })
], MmsTextField.prototype, "maxLength", void 0);
__decorate([
    property({ type: Boolean, attribute: 'show-character-counter' })
], MmsTextField.prototype, "showCharacterCounter", void 0);
__decorate([
    property({ type: Boolean, reflect: true, attribute: 'show-tooltip' })
], MmsTextField.prototype, "showTooltip", void 0);
__decorate([
    property({ type: String, attribute: 'tooltip-text' })
], MmsTextField.prototype, "tooltipText", void 0);
__decorate([
    state()
], MmsTextField.prototype, "_showPassword", void 0);
MmsTextField = __decorate([
    customElement('mms-text-field')
], MmsTextField);
export { MmsTextField };
//# sourceMappingURL=mms-text-field.component.js.map