var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { LitElement, css, html, nothing } from 'lit';
import { customElement, property } from 'lit/decorators.js';
import { ifDefined } from 'lit/directives/if-defined.js';
import { focusRing } from '../../styles/focus.css.js';
import { validateColorScheme } from '../../utils/validate-color-scheme.js';
/**
 * showIcon defaults to true — plain HTML boolean-attribute semantics
 * (presence = true) can't express "false" via a string, which breaks
 * show-icon="false" for static-HTML consumers (AEM/Drupal) that can't use
 * Lit property binding. This converter treats the literal string "false" as
 * false. Same pattern as mms-progress-bar's showLabel.
 */
const defaultTrueBooleanConverter = {
    fromAttribute: (value) => value !== 'false',
    toAttribute: (value) => (value ? null : 'false'),
};
/**
 * Toggle (switch) component for binary on/off settings.
 *
 * @element mms-toggle
 *
 * @prop {string} label - Clickable text next to the toggle
 * @prop {boolean} checked - Whether the toggle is on
 * @prop {ToggleColorScheme} colorScheme - Brand color applied when checked
 * @prop {ToggleState} state - Interactive state preview (for documentation)
 * @prop {boolean} readonly - Prevents changes but remains focusable and visible
 * @prop {boolean} disabled - Prevents interaction and dims appearance
 * @prop {boolean} showIcon - Shows a check/close glyph inside the thumb
 * @prop {string} heading - Optional title displayed above the toggle
 * @prop {string} helperText - Supplementary guidance displayed below
 * @prop {string} fieldName - Identifier used when submitting form data
 * @prop {string} checkedValue - Value submitted to the form when checked
 *
 * @fires change - When the toggle state changes
 */
let MmsToggle = class MmsToggle extends LitElement {
    constructor() {
        super(...arguments);
        // ── Core Interaction ────────────────────────────────────────
        this.label = '';
        this.checked = false;
        // ── Visual ──────────────────────────────────────────────────
        this.colorScheme = 'primary';
        this.state = 'default';
        this.readonly = false;
        this.disabled = false;
        this.showIcon = true;
        // ── Secondary Content ───────────────────────────────────────
        this.heading = '';
        this.helperText = '';
        // ── Form Integration ────────────────────────────────────────
        this.fieldName = '';
        this.checkedValue = 'on';
    }
    // ── Lifecycle ───────────────────────────────────────────────
    firstUpdated() {
        // Validate colorScheme × theme combination
        validateColorScheme(this, this.colorScheme, 'mms-toggle');
    }
    // ── Event Handlers ──────────────────────────────────────────
    _handleChange(e) {
        if (this.disabled || this.readonly) {
            e.preventDefault();
            return;
        }
        const input = e.target;
        this.checked = input.checked;
        this.dispatchEvent(new CustomEvent('change', {
            detail: { checked: this.checked, value: this.checkedValue },
            bubbles: true,
            composed: true,
        }));
    }
    _handleClick(e) {
        if (this.disabled || this.readonly) {
            e.preventDefault();
        }
    }
    // ── Render ──────────────────────────────────────────────────
    get _ariaDescribedBy() {
        return this.helperText ? 'helper-text' : undefined;
    }
    _renderIcon() {
        // Inline SVG rather than mms-icon — no icon-size token exists below
        // --icon-size-sm (16px); Marina's thumb icon is a fixed 10px.
        return this.checked
            ? html `<span class="icon" aria-hidden="true">
          <svg viewBox="0 0 10 10" fill="none">
            <path d="M1 5L4 8L9 1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </span>`
            : html `<span class="icon" aria-hidden="true">
          <svg viewBox="0 0 10 10" fill="none">
            <path d="M1 1L9 9M9 1L1 9" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
          </svg>
        </span>`;
    }
    render() {
        return html `
      ${this.heading ? html `<span class="heading" id="heading">${this.heading}</span>` : nothing}

      <label class="toggle-row" @click=${this._handleClick}>
        <span class="toggle-wrapper">
          <input
            type="checkbox"
            role="switch"
            class="native-input"
            .checked=${this.checked}
            ?disabled=${this.disabled}
            aria-checked=${this.checked ? 'true' : 'false'}
            aria-readonly=${ifDefined(this.readonly ? 'true' : undefined)}
            aria-describedby=${ifDefined(this._ariaDescribedBy)}
            name=${ifDefined(this.fieldName || undefined)}
            value=${this.checkedValue}
            @change=${this._handleChange}
          />
          <span class="track"></span>
          <span class="hover-bg"></span>
          <span class="thumb">${this.showIcon ? this._renderIcon() : nothing}</span>
        </span>
        ${this.label ? html `<span class="label">${this.label}</span>` : nothing}
      </label>

      ${this.helperText ? html `<span class="helper-text" id="helper-text">${this.helperText}</span>` : nothing}
    `;
    }
};
MmsToggle.styles = [
    focusRing,
    css `
      /* ═══════════════════════════════════════════════════════════════════════
         HOST — Color Scheme Mapping
         ═══════════════════════════════════════════════════════════════════════ */
      :host {
        display: inline-flex;
        flex-direction: column;
        gap: var(--spacing-xs2);
        font-family: var(--type-body-md-family);

        /* Default: primary color scheme */
        --_fill: var(--color-primary-9);
        --_fill-hover: var(--color-primary-10, var(--color-primary-9));
        --_hover-border: var(--color-primary-8);
      }

      :host([color-scheme='secondary']) {
        --_fill: var(--color-secondary-9);
        --_fill-hover: var(--color-secondary-10, var(--color-secondary-9));
        --_hover-border: var(--color-secondary-8);
      }

      :host([color-scheme='accent']) {
        --_fill: var(--color-accent-9);
        --_fill-hover: var(--color-accent-10, var(--color-accent-9));
        --_hover-border: var(--color-accent-8, var(--color-accent-9));
      }

      :host([color-scheme='onyx']) {
        --_fill: var(--color-onyx-9);
        --_fill-hover: var(--color-onyx-10, var(--color-onyx-9));
        --_hover-border: var(--color-onyx-8);
      }

      :host([disabled]) {
        cursor: not-allowed;
        pointer-events: none;
      }

      :host([readonly]) {
        cursor: default;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         HEADING — Optional title above toggle
         ═══════════════════════════════════════════════════════════════════════ */
      .heading {
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        color: var(--color-text-default);
        margin-bottom: var(--spacing-xs2);
      }

      :host([disabled]) .heading {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         TOGGLE ROW — Switch + Label
         ═══════════════════════════════════════════════════════════════════════ */
      .toggle-row {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-sm1);
        cursor: pointer;
      }

      :host([disabled]) .toggle-row {
        cursor: not-allowed;
        pointer-events: none;
      }

      :host([readonly]) .toggle-row {
        cursor: default;
        pointer-events: none;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         TOGGLE WRAPPER — Touch target (track height + established
         checkbox touch-target padding convention: visual + 8px)
         ═══════════════════════════════════════════════════════════════════════ */
      .toggle-wrapper {
        position: relative;
        width: var(--size-xl2); /* 48px */
        height: var(--size-lg2); /* 32px touch target (24px track + 8px, per checkbox convention) */
        flex-shrink: 0;
      }

      .native-input {
        position: absolute;
        inset: 0;
        opacity: 0;
        margin: 0;
        cursor: inherit;
        z-index: 2;
      }

      /* Track (background) */
      .track {
        position: absolute;
        top: 4px; /* (32 - 24) / 2 */
        left: 0;
        width: 100%;
        height: var(--size-lg1); /* 24px */
        border-radius: var(--radius-full);
        background: var(--color-border-interactive);
        transition: background var(--motion-duration-fast) var(--motion-easing-standard);
        box-sizing: border-box;
        z-index: 0;
      }

      /* Hover background — centered on thumb. Base state is fully transparent
         (fill + border); on hover it fills to match the thumb's own color
         and gains a colorScheme-scoped border, so the thumb reads as
         enlarging inside a bordered ring rather than showing a separate
         tint behind it (see HOVER STATE below). */
      .hover-bg {
        position: absolute;
        top: 0;
        left: -4px; /* thumb-left(2px) - (halo(32px) - thumb(20px)) / 2 (6px) */
        width: var(--size-lg2); /* 32px */
        height: var(--size-lg2);
        border-radius: 50%;
        background: transparent;
        border: var(--border-width-sm) solid transparent;
        box-sizing: border-box;
        transition:
          background var(--motion-duration-fast) var(--motion-easing-standard),
          border-color var(--motion-duration-fast) var(--motion-easing-standard);
        z-index: 1;
      }

      /* Thumb (the circle that moves) */
      .thumb {
        position: absolute;
        top: 6px; /* (32 - 20) / 2 */
        left: 2px;
        width: var(--size-md2); /* 20px */
        height: var(--size-md2);
        border-radius: 50%;
        background: var(--color-surface-raised);
        display: flex;
        align-items: center;
        justify-content: center;
        transition:
          left var(--motion-duration-fast) var(--motion-easing-standard),
          background var(--motion-duration-fast) var(--motion-easing-standard);
        box-sizing: border-box;
        z-index: 2;
      }

      /* Icon inside thumb — no mms-icon size token exists below 16px
         (--icon-size-sm); drawn as an inline SVG at Marina's native 10px. */
      .icon {
        width: 10px;
        height: 10px;
        color: var(--color-border-interactive);
        transition: color var(--motion-duration-fast) var(--motion-easing-standard);
      }

      .icon svg {
        display: block;
        width: 100%;
        height: 100%;
      }

      /* ═══════════════════════════════════════════════════════════════════════
         CHECKED STATE
         ═══════════════════════════════════════════════════════════════════════ */
      :host([checked]) .track {
        background: var(--_fill);
      }

      :host([checked]) .thumb {
        left: calc(100% - var(--size-md2) - 2px); /* 48 - 20 - 2 */
      }

      :host([checked]) .hover-bg {
        /* Mirrors the thumb's own checked offset (100% - 20px - 2px), minus the
           same 6px halo margin used in the unchecked .hover-bg rule above
           ((32px halo - 20px thumb) / 2). A naive port of Marina's checked-state
           formula (width - haloSize + 2px) only centers correctly for Marina's
           28px halo / 20px thumb ratio (4px margin); mms's 32px halo needs a
           6px margin, so that formula lands 2px off-center here. */
        left: calc(100% - var(--size-md2) - 2px - 6px); /* 48 - 20 - 2 - 6 */
      }

      :host([checked]) .icon {
        color: var(--_fill);
      }

      :host([checked]:not([disabled]):not([readonly])) .toggle-row:hover .thumb,
      :host([checked][state='hover']:not([disabled]):not([readonly])) .thumb {
        background: var(--color-surface-raised);
      }

      :host([checked]:not([disabled]):not([readonly])) .toggle-row:hover .track,
      :host([checked][state='hover']:not([disabled]):not([readonly])) .track {
        background: var(--_fill-hover);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         HOVER STATE (EXPERIMENT) — hover-bg fill matches the thumb's own
         color (--color-surface-raised), so hover reads as the thumb circle
         enlarging inside a colorScheme-scoped border ring (--_hover-border,
         which resolves to that scheme's own -8 step) rather than a separate
         background tint.
         ═══════════════════════════════════════════════════════════════════════ */
      :host(:not([disabled]):not([readonly])) .toggle-row:hover .hover-bg,
      :host([state='hover']:not([disabled]):not([readonly])) .hover-bg {
        background: var(--color-surface-raised);
        border-color: var(--_hover-border);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         FOCUS STATE
         ═══════════════════════════════════════════════════════════════════════ */
      .native-input:focus-visible ~ .track,
      :host([state='focus']) .track {
        outline: var(--focus-ring-width) var(--focus-ring-style) var(--focus-ring-color);
        outline-offset: var(--focus-ring-offset);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         DISABLED STATE
         ═══════════════════════════════════════════════════════════════════════ */
      :host([disabled]) .track {
        background: var(--color-disabled-surface);
      }

      :host([disabled]) .thumb {
        background: var(--color-disabled-stroke);
      }

      :host([disabled][checked]) .track {
        background: var(--color-disabled-surface);
      }

      :host([disabled]) .icon {
        color: var(--color-surface-raised);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         READONLY STATE
         ═══════════════════════════════════════════════════════════════════════ */
      :host([readonly]) .track {
        background: transparent;
        border: var(--border-width-xs) solid var(--color-disabled-stroke);
      }

      :host([readonly]) .thumb {
        border: var(--border-width-xs) solid var(--color-disabled-stroke);
      }

      :host([readonly][checked]) .track {
        background: transparent;
        border: var(--border-width-xs) solid var(--color-disabled-stroke);
      }

      :host([readonly]) .icon {
        color: var(--color-text-default);
      }

      :host([readonly][checked]) .icon {
        color: var(--color-text-default);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         LABEL TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .label {
        font-size: var(--type-body-md-size);
        line-height: var(--type-body-md-line-height);
        color: var(--color-text-default);
        user-select: none;
      }

      :host([disabled]) .label {
        color: var(--color-disabled-text);
      }

      /* ═══════════════════════════════════════════════════════════════════════
         HELPER TEXT
         ═══════════════════════════════════════════════════════════════════════ */
      .helper-text {
        font-size: var(--type-ui-caption-size);
        line-height: var(--type-ui-caption-line-height);
        color: var(--color-text-subtle);
      }

      :host([disabled]) .helper-text {
        color: var(--color-disabled-text);
      }
    `,
];
__decorate([
    property({ type: String })
], MmsToggle.prototype, "label", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsToggle.prototype, "checked", void 0);
__decorate([
    property({ type: String, reflect: true, attribute: 'color-scheme' })
], MmsToggle.prototype, "colorScheme", void 0);
__decorate([
    property({ type: String, reflect: true })
], MmsToggle.prototype, "state", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsToggle.prototype, "readonly", void 0);
__decorate([
    property({ type: Boolean, reflect: true })
], MmsToggle.prototype, "disabled", void 0);
__decorate([
    property({ reflect: true, attribute: 'show-icon', converter: defaultTrueBooleanConverter })
], MmsToggle.prototype, "showIcon", void 0);
__decorate([
    property({ type: String })
], MmsToggle.prototype, "heading", void 0);
__decorate([
    property({ type: String, attribute: 'helper-text' })
], MmsToggle.prototype, "helperText", void 0);
__decorate([
    property({ type: String, attribute: 'field-name' })
], MmsToggle.prototype, "fieldName", void 0);
__decorate([
    property({ type: String, attribute: 'checked-value' })
], MmsToggle.prototype, "checkedValue", void 0);
MmsToggle = __decorate([
    customElement('mms-toggle')
], MmsToggle);
export { MmsToggle };
// Ensure registration for production builds (Vite tree-shaking safeguard)
if (!customElements.get('mms-toggle')) {
    customElements.define('mms-toggle', MmsToggle);
}
//# sourceMappingURL=mms-toggle.component.js.map