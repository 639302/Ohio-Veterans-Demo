/**
 * The canonical `alert-type` enum, shared by mms-status-tag, mms-inline-alert,
 * mms-alert-banner and mms-toast. All four declare their own named type alias for
 * documentation purposes; those aliases are the same four members as this list, and
 * this is the single runtime copy.
 */
export const ALERT_TYPES = ['success', 'error', 'caution', 'informational'];
/**
 * Alert-type aliases.
 *
 * The public `alert-type` enum uses `caution`. The Phosphor icon asset rendered for
 * that state is named `warning`, and `warning` is the more common vocabulary across
 * design systems — so a consumer (or their AI) reasonably guesses it as the attribute
 * value.
 *
 * `warning` is not a valid enum member. Without this alias it misses every
 * `:host([alert-type=…])` block and every icon `switch`, and the component silently
 * renders its default state — a success or informational tag where the author asked
 * for a caution.
 */
const ALERT_TYPE_ALIASES = {
    warning: 'caution',
};
/**
 * Resolves a known alias, then clamps to `fallback` if the result is not a valid enum
 * member. Callers apply this on the property in `willUpdate`, not at the point of icon
 * selection, so the reflected attribute, the CSS selector match and the icon stay in
 * agreement — clamping only the icon would leave CSS matching nothing and render an
 * unstyled surface with a valid-looking icon.
 *
 * `fallback` must be the component's own property default, so that valid usage is
 * unaffected and an invalid value degrades to exactly what an unset attribute renders.
 *
 * This clamps silently rather than logging. A wrong-status render is the visible
 * symptom — an author who asks for `eror` sees an informational alert where they
 * expected an error one. Catching the typo at author time is the job of the
 * package-wide enum validation ticket, not of 55 hand-written runtime guards.
 */
export function normalizeAlertType(value, fallback) {
    const resolved = ALERT_TYPE_ALIASES[value] ?? value;
    return ALERT_TYPES.includes(resolved) ? resolved : fallback;
}
//# sourceMappingURL=normalize-alert-type.js.map